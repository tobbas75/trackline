---
phase: 02-monorepo-workspace
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "C:/Software code GITs/LandManagment Website/workspace.code-workspace"
autonomous: true
requirements:
  - MONO-01
must_haves:
  truths:
    - "pnpm is installed and `pnpm --version` prints 10.x"
    - "camera-trap-dashboard/ exists at LandManagment Website/camera-trap-dashboard/ with its .git directory intact"
    - "fire-app/ exists at LandManagment Website/fire-app/ with its .git directory intact"
    - "trap-monitor/ exists at LandManagment Website/trap-monitor/ with its .git directory intact"
    - "workspace.code-workspace references the new relative paths inside LandManagment Website/"
  artifacts:
    - path: "C:/Software code GITs/LandManagment Website/camera-trap-dashboard"
      provides: "WildTrack app at workspace root"
    - path: "C:/Software code GITs/LandManagment Website/fire-app"
      provides: "Fire System app at workspace root"
    - path: "C:/Software code GITs/LandManagment Website/trap-monitor"
      provides: "Trap Monitor frontend at workspace root"
    - path: "C:/Software code GITs/LandManagment Website/workspace.code-workspace"
      provides: "Updated VS Code workspace with corrected paths"
  key_links:
    - from: "pnpm-workspace.yaml (future plan 02)"
      to: "camera-trap-dashboard/, fire-app/, trap-monitor/"
      via: "glob patterns relative to workspace root"
      pattern: "all apps must be inside LandManagment Website/"
---

<objective>
Install pnpm and move the three satellite app directories inside the workspace root.

Purpose: pnpm workspaces cannot reference packages outside the workspace root directory (confirmed open bug pnpm/pnpm #10096). All four apps must live under `LandManagment Website/` for `pnpm install` to link them correctly as workspace packages.

Output: pnpm installed globally; camera-trap-dashboard, fire-app, and trap-monitor directories moved inside `LandManagment Website/`; VS Code workspace file updated to match new paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pnpm globally</name>
  <files>N/A — system install</files>
  <read_first>
    - Run `pnpm --version` first; if it returns 10.x, skip installation.
    - Run `winget --version` to confirm winget is available (it is: 1.28.220).
  </read_first>
  <action>
    Install pnpm v10 using winget (preferred on Windows — avoids npm global permission issues):

    ```
    winget install pnpm.pnpm
    ```

    After install, close and reopen the shell or run `refreshenv` to pick up the new PATH entry.
    Verify: `pnpm --version` must print 10.x.

    If winget install fails (permissions or network), fall back to:
    ```
    npm install -g pnpm@10
    ```

    Do NOT use `corepack enable` path unless both winget and npm install fail — it requires admin.
  </action>
  <verify>
    <automated>pnpm --version</automated>
  </verify>
  <done>pnpm --version prints a version starting with 10.</done>
</task>

<task type="auto">
  <name>Task 2: Move satellite apps into workspace root</name>
  <files>
    C:/Software code GITs/LandManagment Website/camera-trap-dashboard/
    C:/Software code GITs/LandManagment Website/fire-app/
    C:/Software code GITs/LandManagment Website/trap-monitor/
    C:/Software code GITs/LandManagment Website/workspace.code-workspace
  </files>
  <read_first>
    Read C:/Software code GITs/LandManagment Website/workspace.code-workspace to understand current path references.

    Verify current locations before moving:
    - `ls "c:/Software code GITs/camera-trap-dashboard/"` — must exist
    - `ls "c:/Software code GITs/Fire project system/fire-app/"` — must exist
    - `ls "c:/Software code GITs/Trap Monitor/frontend/"` — must exist

    Verify git roots:
    - `git -C "c:/Software code GITs/camera-trap-dashboard" rev-parse --show-toplevel` → must return `C:/Software code GITs/camera-trap-dashboard`
    - `git -C "c:/Software code GITs/Fire project system/fire-app" rev-parse --show-toplevel` → must return `C:/Software code GITs/Fire project system/fire-app`
    - `git -C "c:/Software code GITs/Trap Monitor" rev-parse --show-toplevel` → must return `C:/Software code GITs/Trap Monitor` (NOTE: Trap Monitor git root is the parent, not frontend/)
  </read_first>
  <action>
    **IMPORTANT:** Trap Monitor's git root is `C:/Software code GITs/Trap Monitor/` (not `frontend/`). Moving only `frontend/` would leave a broken git repo. You must move the entire `Trap Monitor/` directory, then rename it to `trap-monitor` inside the workspace root.

    Execute these moves using Bash (PowerShell `mv` handles directories with contents):

    Step 1 — Move camera-trap-dashboard:
    ```bash
    mv "c:/Software code GITs/camera-trap-dashboard" "c:/Software code GITs/LandManagment Website/camera-trap-dashboard"
    ```

    Step 2 — Move fire-app (just the app directory; fire-app is its own git root):
    ```bash
    mv "c:/Software code GITs/Fire project system/fire-app" "c:/Software code GITs/LandManagment Website/fire-app"
    ```

    Step 3 — Move Trap Monitor (entire directory, git root is Trap Monitor/):
    ```bash
    mv "c:/Software code GITs/Trap Monitor" "c:/Software code GITs/LandManagment Website/trap-monitor"
    ```

    After the moves, the workspace app layout will be:
    ```
    LandManagment Website/
    ├── portal/                   ← existing (no move needed)
    ├── camera-trap-dashboard/    ← moved from C:/Software code GITs/camera-trap-dashboard/
    ├── fire-app/                 ← moved from C:/Software code GITs/Fire project system/fire-app/
    ├── trap-monitor/             ← moved from C:/Software code GITs/Trap Monitor/ (git root preserved)
    │   └── frontend/             ← trap-monitor Next.js app is here
    └── workspace.code-workspace
    ```

    Step 4 — Update workspace.code-workspace to use new relative paths.

    The file currently contains:
    ```json
    {
      "folders": [
        { "name": "Portal", "path": "portal" },
        { "name": "WildTrack", "path": "../camera-trap-dashboard" },
        { "name": "Fire System", "path": "../Fire project system/fire-app" },
        { "name": "Trap Monitor", "path": "../Trap Monitor/frontend" }
      ]
    }
    ```

    Update it to:
    ```json
    {
      "folders": [
        { "name": "Portal", "path": "portal" },
        { "name": "WildTrack", "path": "camera-trap-dashboard" },
        { "name": "Fire System", "path": "fire-app" },
        { "name": "Trap Monitor", "path": "trap-monitor/frontend" }
      ],
      "settings": {
        "files.exclude": {
          "**/node_modules": true,
          "**/.next": true
        }
      }
    }
    ```

    Note: The `settings` block is preserved from the original file.
  </action>
  <verify>
    <automated>
      ls "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/package.json" &&
      ls "c:/Software code GITs/LandManagment Website/fire-app/package.json" &&
      ls "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/package.json" &&
      git -C "c:/Software code GITs/LandManagment Website/camera-trap-dashboard" rev-parse --show-toplevel &&
      git -C "c:/Software code GITs/LandManagment Website/fire-app" rev-parse --show-toplevel &&
      git -C "c:/Software code GITs/LandManagment Website/trap-monitor" rev-parse --show-toplevel
    </automated>
  </verify>
  <done>
    - camera-trap-dashboard/package.json exists at LandManagment Website/camera-trap-dashboard/
    - fire-app/package.json exists at LandManagment Website/fire-app/
    - trap-monitor/frontend/package.json exists at LandManagment Website/trap-monitor/frontend/
    - All three git rev-parse calls return paths inside LandManagment Website/
    - workspace.code-workspace paths are updated (no ../ references)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `pnpm --version` → prints 10.x
2. All three satellite apps exist inside `c:/Software code GITs/LandManagment Website/`
3. Git repos intact: `git -C .../camera-trap-dashboard log --oneline -1`, `git -C .../fire-app log --oneline -1`, `git -C .../trap-monitor log --oneline -1` all return a commit hash
4. workspace.code-workspace contains no `../` path references
</verification>

<success_criteria>
- pnpm 10.x is installed globally
- Three satellite app directories exist inside LandManagment Website/ with their .git directories
- workspace.code-workspace opens correctly in VS Code (paths resolve to app directories)
- No app source files were lost or corrupted during the move
</success_criteria>

<output>
After completion, create `.planning/phases/02-monorepo-workspace/02-01-SUMMARY.md` following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
