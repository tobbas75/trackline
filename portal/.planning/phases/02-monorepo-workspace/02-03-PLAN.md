---
phase: 02-monorepo-workspace
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - "C:/Software code GITs/LandManagment Website/portal/tsconfig.json"
  - "C:/Software code GITs/LandManagment Website/portal/eslint.config.mjs"
  - "C:/Software code GITs/LandManagment Website/portal/package.json"
  - "C:/Software code GITs/LandManagment Website/camera-trap-dashboard/tsconfig.json"
  - "C:/Software code GITs/LandManagment Website/camera-trap-dashboard/eslint.config.mjs"
  - "C:/Software code GITs/LandManagment Website/camera-trap-dashboard/package.json"
  - "C:/Software code GITs/LandManagment Website/fire-app/tsconfig.json"
  - "C:/Software code GITs/LandManagment Website/fire-app/eslint.config.mjs"
  - "C:/Software code GITs/LandManagment Website/fire-app/package.json"
  - "C:/Software code GITs/LandManagment Website/trap-monitor/frontend/tsconfig.json"
  - "C:/Software code GITs/LandManagment Website/trap-monitor/frontend/eslint.config.mjs"
  - "C:/Software code GITs/LandManagment Website/trap-monitor/frontend/package.json"
autonomous: true
requirements:
  - MONO-02
  - MONO-03
  - MONO-05
must_haves:
  truths:
    - "Every app's tsconfig.json extends @trackline/tsconfig/base.json and retains only app-specific fields"
    - "Every app's eslint.config.mjs imports from @trackline/eslint-config (trap-monitor gets a new eslint.config.mjs)"
    - "Every app's package.json uses catalog: references for shared dependencies"
    - "Every app's package.json has a typecheck script running tsc --noEmit"
  artifacts:
    - path: "C:/Software code GITs/LandManagment Website/portal/tsconfig.json"
      provides: "Portal tsconfig extending shared base"
      contains: "@trackline/tsconfig/base.json"
    - path: "C:/Software code GITs/LandManagment Website/camera-trap-dashboard/tsconfig.json"
      provides: "WildTrack tsconfig extending shared base"
      contains: "@trackline/tsconfig/base.json"
    - path: "C:/Software code GITs/LandManagment Website/fire-app/tsconfig.json"
      provides: "Fire System tsconfig extending shared base"
      contains: "@trackline/tsconfig/base.json"
    - path: "C:/Software code GITs/LandManagment Website/trap-monitor/frontend/tsconfig.json"
      provides: "Trap Monitor tsconfig extending shared base"
      contains: "@trackline/tsconfig/base.json"
  key_links:
    - from: "all 4 app tsconfig.json"
      to: "packages/tsconfig/tsconfig.base.json"
      via: "extends: @trackline/tsconfig/base.json"
      pattern: "@trackline/tsconfig/base.json"
    - from: "all 4 app eslint.config.mjs"
      to: "packages/eslint-config/index.mjs"
      via: "import { nextConfig } from @trackline/eslint-config"
      pattern: "@trackline/eslint-config"
---

<objective>
Migrate all four apps to extend the shared packages created in Plan 02: update tsconfig.json to extend @trackline/tsconfig/base.json, update eslint.config.mjs to import from @trackline/eslint-config, and update package.json dependencies to use `catalog:` references.

Purpose: Eliminates copy-paste drift. After this plan, changing a shared tsconfig option or ESLint rule in packages/ propagates to all four apps automatically.

Output: 12 modified files (tsconfig.json, eslint.config.mjs, package.json for each of 4 apps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-monorepo-workspace/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Shared package names from Plan 02 (use exactly these): -->
<!--   @trackline/tsconfig   — exports "./base.json": "./tsconfig.base.json" -->
<!--   @trackline/eslint-config — exports ".": "./index.mjs", named export: nextConfig -->

<!-- tsconfig.base.json contains (Plan 02): -->
<!--   target, lib, allowJs, skipLibCheck, strict, noEmit, esModuleInterop, -->
<!--   module, moduleResolution, resolveJsonModule, isolatedModules, jsx -->
<!-- App tsconfig.json must keep (NOT in base): -->
<!--   incremental, plugins: [{name: "next"}], paths: {"@/*": ["./src/*"]}, include, exclude -->

<!-- package.json catalog: references use exactly the key names from pnpm-workspace.yaml catalog: -->
<!--   "react": "catalog:", "next": "catalog:", "@supabase/ssr": "catalog:", etc. -->

<!-- Trap Monitor package.json lint script: currently "next lint" — change to "eslint" -->
<!-- Trap Monitor has NO eslint.config.mjs — must CREATE it (not update) -->

<!-- App locations after Plan 01 moves: -->
<!--   portal         → LandManagment Website/portal/ -->
<!--   camera-trap    → LandManagment Website/camera-trap-dashboard/ -->
<!--   fire-app       → LandManagment Website/fire-app/ -->
<!--   trap-monitor   → LandManagment Website/trap-monitor/frontend/ -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate portal, camera-trap-dashboard, and fire-app configs</name>
  <files>
    C:/Software code GITs/LandManagment Website/portal/tsconfig.json
    C:/Software code GITs/LandManagment Website/portal/eslint.config.mjs
    C:/Software code GITs/LandManagment Website/portal/package.json
    C:/Software code GITs/LandManagment Website/camera-trap-dashboard/tsconfig.json
    C:/Software code GITs/LandManagment Website/camera-trap-dashboard/eslint.config.mjs
    C:/Software code GITs/LandManagment Website/camera-trap-dashboard/package.json
    C:/Software code GITs/LandManagment Website/fire-app/tsconfig.json
    C:/Software code GITs/LandManagment Website/fire-app/eslint.config.mjs
    C:/Software code GITs/LandManagment Website/fire-app/package.json
  </files>
  <read_first>
    Read each file before modifying it:
    - C:/Software code GITs/LandManagment Website/portal/tsconfig.json
    - C:/Software code GITs/LandManagment Website/portal/eslint.config.mjs
    - C:/Software code GITs/LandManagment Website/portal/package.json
    - C:/Software code GITs/LandManagment Website/camera-trap-dashboard/tsconfig.json
    - C:/Software code GITs/LandManagment Website/camera-trap-dashboard/eslint.config.mjs
    - C:/Software code GITs/LandManagment Website/camera-trap-dashboard/package.json
    - C:/Software code GITs/LandManagment Website/fire-app/tsconfig.json
    - C:/Software code GITs/LandManagment Website/fire-app/eslint.config.mjs
    - C:/Software code GITs/LandManagment Website/fire-app/package.json
  </read_first>
  <action>
    Apply identical transformations to portal, camera-trap-dashboard, and fire-app.

    ---

    **A. tsconfig.json — for each of the 3 apps**

    Replace the current content with a slim app-level tsconfig that extends the shared base.
    Keep ONLY the fields that are app-specific: `extends`, `compilerOptions` (incremental, plugins,
    paths), `include`, `exclude`.

    The `include` array differs slightly per app — preserve what each app currently has.
    Portal currently has: `["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", ".next/dev/types/**/*.ts", "**/*.mts"]`
    Use the same pattern for camera-trap-dashboard and fire-app (read their files to get the exact current value).

    New content for each app's tsconfig.json:
    ```json
    {
      "extends": "@trackline/tsconfig/base.json",
      "compilerOptions": {
        "incremental": true,
        "plugins": [{ "name": "next" }],
        "paths": { "@/*": ["./src/*"] }
      },
      "include": [<PRESERVE EXISTING include array for this app>],
      "exclude": ["node_modules"]
    }
    ```

    ---

    **B. eslint.config.mjs — for each of the 3 apps**

    Replace the existing content with a one-liner that re-exports the shared config.
    These 3 apps already have the right config structure — we are replacing it with an import:

    ```js
    import { nextConfig } from "@trackline/eslint-config";
    export default nextConfig;
    ```

    ---

    **C. package.json — for each of the 3 apps**

    Three changes:
    1. Add `"@trackline/tsconfig": "workspace:*"` to devDependencies
    2. Add `"@trackline/eslint-config": "workspace:*"` to devDependencies
    3. Replace all deps that appear in the catalog with `"catalog:"` references
    4. Ensure a `"typecheck": "tsc --noEmit"` script exists (portal already has it; add to others if missing)

    Catalog replacements (use EXACTLY these keys from pnpm-workspace.yaml):
    - `"react"` → `"catalog:"`
    - `"react-dom"` → `"catalog:"`
    - `"next"` → `"catalog:"`
    - `"@supabase/ssr"` → `"catalog:"`
    - `"@supabase/supabase-js"` → `"catalog:"`
    - `"tailwindcss"` → `"catalog:"`
    - `"@tailwindcss/postcss"` → `"catalog:"`
    - `"typescript"` → `"catalog:"`
    - `"eslint"` → `"catalog:"`
    - `"eslint-config-next"` → `"catalog:"`
    - `"lucide-react"` → `"catalog:"` (if present)
    - `"clsx"` → `"catalog:"` (if present)
    - `"tailwind-merge"` → `"catalog:"` (if present)
    - `"@types/react"` → `"catalog:"`
    - `"@types/react-dom"` → `"catalog:"` (if present)
    - `"@types/node"` → `"catalog:"`

    Deps NOT in the catalog (keep their exact current versions):
    - All app-specific deps: @tanstack/react-query, @tanstack/react-table, recharts, leaflet,
      react-leaflet, react-hook-form, @hookform/resolvers, radix-ui, @maplibre/* packages,
      @turf/* packages, vitest, @testing-library/*, tsx, date-fns, papaparse, etc.
  </action>
  <verify>
    <automated>
      grep -l "@trackline/tsconfig/base.json" "c:/Software code GITs/LandManagment Website/portal/tsconfig.json" "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/tsconfig.json" "c:/Software code GITs/LandManagment Website/fire-app/tsconfig.json" &&
      grep -l "@trackline/eslint-config" "c:/Software code GITs/LandManagment Website/portal/eslint.config.mjs" "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/eslint.config.mjs" "c:/Software code GITs/LandManagment Website/fire-app/eslint.config.mjs" &&
      grep "catalog:" "c:/Software code GITs/LandManagment Website/portal/package.json"
    </automated>
  </verify>
  <done>
    - All 3 app tsconfig.json files contain `"extends": "@trackline/tsconfig/base.json"` and do NOT contain `"strict"` (that's now in the base)
    - All 3 app eslint.config.mjs files contain `@trackline/eslint-config`
    - All 3 app package.json files contain `"catalog:"` for shared deps and `workspace:*` for @trackline packages
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate trap-monitor/frontend configs</name>
  <files>
    C:/Software code GITs/LandManagment Website/trap-monitor/frontend/tsconfig.json
    C:/Software code GITs/LandManagment Website/trap-monitor/frontend/eslint.config.mjs
    C:/Software code GITs/LandManagment Website/trap-monitor/frontend/package.json
  </files>
  <read_first>
    Read each file before modifying:
    - C:/Software code GITs/LandManagment Website/trap-monitor/frontend/tsconfig.json
    - C:/Software code GITs/LandManagment Website/trap-monitor/frontend/package.json
    - Check if C:/Software code GITs/LandManagment Website/trap-monitor/frontend/eslint.config.mjs exists: run `ls "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/"` — it likely does NOT exist (trap-monitor uses `next lint`).
  </read_first>
  <action>
    Trap Monitor needs slightly different handling because it previously had no ESLint flat config.

    ---

    **A. tsconfig.json**

    Same transformation as Task 1. Replace with slim extending config.
    Trap Monitor's current include omits `**/*.mts` (keep that difference — don't add it):

    ```json
    {
      "extends": "@trackline/tsconfig/base.json",
      "compilerOptions": {
        "incremental": true,
        "plugins": [{ "name": "next" }],
        "paths": { "@/*": ["./src/*"] }
      },
      "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
      "exclude": ["node_modules"]
    }
    ```

    Read the current tsconfig.json first and preserve the exact include array it has.

    ---

    **B. eslint.config.mjs — CREATE (does not exist yet)**

    ```js
    import { nextConfig } from "@trackline/eslint-config";
    export default nextConfig;
    ```

    ---

    **C. package.json**

    Three changes (same as Task 1) PLUS update the lint script:

    1. Add `"@trackline/tsconfig": "workspace:*"` to devDependencies
    2. Add `"@trackline/eslint-config": "workspace:*"` to devDependencies
    3. Replace catalog deps with `"catalog:"` (same list as Task 1)
    4. Add `"typecheck": "tsc --noEmit"` to scripts (trap-monitor currently has no typecheck script)
    5. Change `"lint": "next lint"` → `"lint": "eslint"` (per Pitfall 3 in research: `next lint` uses legacy built-in config, not our shared flat config)

    Catalog replacements for trap-monitor (apply to deps present in trap-monitor's package.json):
    - `"react"` → `"catalog:"`
    - `"react-dom"` → `"catalog:"`
    - `"next"` → `"catalog:"`
    - `"@supabase/ssr"` → `"catalog:"`
    - `"@supabase/supabase-js"` → `"catalog:"`
    - `"tailwindcss"` → `"catalog:"` (it's in devDependencies as @tailwindcss/postcss)
    - `"@tailwindcss/postcss"` → `"catalog:"`
    - `"typescript"` → `"catalog:"` (check if in devDeps)
    - `"eslint"` → `"catalog:"` (add if not present — needed for `eslint` CLI)
    - `"eslint-config-next"` → `"catalog:"` (add if not present — peerDep of @trackline/eslint-config)
    - `"clsx"` → `"catalog:"`
    - `"@types/react"` → `"catalog:"`
    - `"@types/react-dom"` → `"catalog:"` (if present)
    - `"@types/node"` → `"catalog:"` (if present)

    IMPORTANT: When adding `eslint` and `eslint-config-next` to trap-monitor devDependencies,
    use `"catalog:"` (not a version number) — the catalog defines the version.
  </action>
  <verify>
    <automated>
      grep "@trackline/tsconfig/base.json" "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/tsconfig.json" &&
      grep "@trackline/eslint-config" "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/eslint.config.mjs" &&
      grep '"lint": "eslint"' "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/package.json" &&
      grep '"typecheck"' "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/package.json"
    </automated>
  </verify>
  <done>
    - trap-monitor/frontend/tsconfig.json extends @trackline/tsconfig/base.json
    - trap-monitor/frontend/eslint.config.mjs exists and imports @trackline/eslint-config
    - trap-monitor/frontend/package.json has lint script as "eslint" (not "next lint")
    - trap-monitor/frontend/package.json has typecheck script as "tsc --noEmit"
    - trap-monitor/frontend/package.json has @trackline/tsconfig and @trackline/eslint-config in devDependencies
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Grep for extends in all 4 tsconfig.json files — all must contain `@trackline/tsconfig/base.json`
2. Grep all 4 eslint.config.mjs files — all must contain `@trackline/eslint-config`
3. None of the 4 tsconfig.json files should contain `"strict"` as a top-level field (it's in the base)
4. trap-monitor/frontend/package.json lint script must be `"eslint"` not `"next lint"`
5. All 4 package.json files must contain at least one `"catalog:"` entry
</verification>

<success_criteria>
- All 4 apps extend @trackline/tsconfig/base.json — no app re-defines the shared compilerOptions
- All 4 apps import from @trackline/eslint-config — single source of truth for ESLint rules
- All 4 apps use catalog: references for the 17 shared dependencies
- All 4 apps have a typecheck script (prerequisite for Plan 04's workspace-wide typecheck verification)
</success_criteria>

<output>
After completion, create `.planning/phases/02-monorepo-workspace/02-03-SUMMARY.md` following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
