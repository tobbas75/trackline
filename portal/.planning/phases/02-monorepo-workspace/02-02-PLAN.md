---
phase: 02-monorepo-workspace
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - "C:/Software code GITs/LandManagment Website/pnpm-workspace.yaml"
  - "C:/Software code GITs/LandManagment Website/package.json"
  - "C:/Software code GITs/LandManagment Website/.npmrc"
  - "C:/Software code GITs/LandManagment Website/packages/tsconfig/package.json"
  - "C:/Software code GITs/LandManagment Website/packages/tsconfig/tsconfig.base.json"
  - "C:/Software code GITs/LandManagment Website/packages/eslint-config/package.json"
  - "C:/Software code GITs/LandManagment Website/packages/eslint-config/index.mjs"
autonomous: true
requirements:
  - MONO-02
  - MONO-03
  - MONO-04
  - MONO-05
must_haves:
  truths:
    - "pnpm-workspace.yaml at workspace root lists all 4 apps and packages/* with catalog entries for all shared deps"
    - "packages/tsconfig/tsconfig.base.json contains the shared compilerOptions (strict, ES2017, bundler resolution) without app-specific fields"
    - "packages/eslint-config/index.mjs exports the nextConfig flat config array"
    - "Root package.json has typecheck, lint, build:*, and dev:* scripts using pnpm -r and --filter"
  artifacts:
    - path: "C:/Software code GITs/LandManagment Website/pnpm-workspace.yaml"
      provides: "Workspace package globs + version catalog"
      contains: "catalog:"
    - path: "C:/Software code GITs/LandManagment Website/packages/tsconfig/tsconfig.base.json"
      provides: "Shared TS compilerOptions"
      contains: "\"strict\": true"
    - path: "C:/Software code GITs/LandManagment Website/packages/eslint-config/index.mjs"
      provides: "Shared ESLint flat config"
      contains: "nextConfig"
    - path: "C:/Software code GITs/LandManagment Website/package.json"
      provides: "Root workspace scripts"
      contains: "trackline-workspace"
  key_links:
    - from: "packages/tsconfig/package.json"
      to: "packages/tsconfig/tsconfig.base.json"
      via: "exports field: \"./base.json\": \"./tsconfig.base.json\""
      pattern: "\"./base.json\""
    - from: "packages/eslint-config/package.json"
      to: "packages/eslint-config/index.mjs"
      via: "exports field: \".\": \"./index.mjs\""
      pattern: "\"./index.mjs\""
---

<objective>
Create the workspace root infrastructure: pnpm-workspace.yaml with catalog, root package.json, .npmrc, and the two shared internal packages (packages/tsconfig and packages/eslint-config).

Purpose: These files define the workspace topology and shared conventions that all four apps will extend in Plan 03. They must exist before any app migration can happen.

Output: Seven new files; no existing app files modified yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-monorepo-workspace/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact package names from app package.json files (verified from codebase) -->
<!-- portal → "portal" -->
<!-- camera-trap-dashboard → "camera-trap-dashboard" -->
<!-- fire-app → "fire-app" -->
<!-- trap-monitor → "trap-monitor-dashboard" -->
<!-- trap-monitor frontend is at trap-monitor/frontend/ (see plan 01) -->

<!-- Verified dependency versions from all 4 app package.json files: -->
<!--   react: 19.2.3 (portal/wildtrack/fire exact; trap-monitor ^19.2.3) -->
<!--   next: 16.1.6 (portal/wildtrack/fire exact; trap-monitor ^16.1.6) -->
<!--   @supabase/ssr: ^0.9.0 (all 4) -->
<!--   @supabase/supabase-js: ^2.98.0 (all 4) -->
<!--   tailwindcss: ^4 (portal/wildtrack/fire); ^4.0.0 (trap-monitor) — catalog: ^4 -->
<!--   @tailwindcss/postcss: ^4 (portal/wildtrack/fire); ^4.0.0 (trap-monitor) — catalog: ^4 -->
<!--   typescript: ^5 (all 4) -->
<!--   eslint: ^9 (all 4) -->
<!--   eslint-config-next: 16.1.6 (portal/wildtrack/fire exact; trap-monitor ^16.1.6) — catalog: 16.1.6 -->
<!--   lucide-react: ^0.577.0 (portal), ^0.576.0 (wildtrack), ^0.575.0 (fire), absent (trap) — catalog: ^0.577.0 -->
<!--   clsx: absent (portal), ^2.1.1 (wildtrack/fire), ^2.1.0 (trap) — catalog: ^2.1.1 -->
<!--   tailwind-merge: absent (portal/trap), ^3.5.0 (wildtrack/fire) — catalog: ^3.5.0 -->
<!--   @types/react: ^19 (all 4) -->
<!--   @types/react-dom: ^19 (all 4) -->
<!--   @types/node: ^20 (all 4) -->

<!-- Shared tsconfig compilerOptions (identical across all 4 apps, verified): -->
<!--   target: ES2017, lib: [dom, dom.iterable, esnext], allowJs: true, skipLibCheck: true -->
<!--   strict: true, noEmit: true, esModuleInterop: true, module: esnext -->
<!--   moduleResolution: bundler, resolveJsonModule: true, isolatedModules: true, jsx: react-jsx -->
<!-- App-specific (NOT in base): incremental, plugins: [{name: "next"}], paths: {"@/*": ["./src/*"]} -->

<!-- Portal eslint.config.mjs (identical to wildtrack/fire — this is the template): -->
<!--   import { defineConfig, globalIgnores } from "eslint/config"; -->
<!--   import nextVitals from "eslint-config-next/core-web-vitals"; -->
<!--   import nextTs from "eslint-config-next/typescript"; -->
<!--   defineConfig([...nextVitals, ...nextTs, globalIgnores([".next/**","out/**","build/**","next-env.d.ts"])]) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace root files (pnpm-workspace.yaml, package.json, .npmrc)</name>
  <files>
    C:/Software code GITs/LandManagment Website/pnpm-workspace.yaml
    C:/Software code GITs/LandManagment Website/package.json
    C:/Software code GITs/LandManagment Website/.npmrc
  </files>
  <read_first>
    - Run `ls "c:/Software code GITs/LandManagment Website/"` to verify the satellite apps from Plan 01 are in place (camera-trap-dashboard/, fire-app/, trap-monitor/ must exist).
    - Confirm trap-monitor app path: `ls "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/package.json"` — the Next.js app is nested at frontend/.
  </read_first>
  <action>
    Create three files at the workspace root (`C:/Software code GITs/LandManagment Website/`).

    **File 1: pnpm-workspace.yaml**

    ```yaml
    packages:
      - 'portal'
      - 'packages/*'
      - 'camera-trap-dashboard'
      - 'fire-app'
      - 'trap-monitor/frontend'

    catalog:
      react: '19.2.3'
      react-dom: '19.2.3'
      next: '16.1.6'
      '@supabase/ssr': '^0.9.0'
      '@supabase/supabase-js': '^2.98.0'
      tailwindcss: '^4'
      '@tailwindcss/postcss': '^4'
      typescript: '^5'
      eslint: '^9'
      'eslint-config-next': '16.1.6'
      'lucide-react': '^0.577.0'
      clsx: '^2.1.1'
      'tailwind-merge': '^3.5.0'
      '@types/react': '^19'
      '@types/react-dom': '^19'
      '@types/node': '^20'
    ```

    Note on trap-monitor path: The Trap Monitor git root is at `trap-monitor/` but the Next.js app (with package.json name `trap-monitor-dashboard`) is at `trap-monitor/frontend/`. The packages entry must point to the directory containing the app's package.json.

    **File 2: package.json**

    ```json
    {
      "name": "trackline-workspace",
      "version": "0.0.1",
      "private": true,
      "packageManager": "pnpm@10.33.0",
      "scripts": {
        "typecheck": "pnpm -r run typecheck",
        "lint": "pnpm -r run lint",
        "build:portal": "pnpm --filter portal build",
        "build:wildtrack": "pnpm --filter camera-trap-dashboard build",
        "build:fire": "pnpm --filter fire-app build",
        "build:trap": "pnpm --filter trap-monitor-dashboard build",
        "dev:portal": "pnpm --filter portal dev",
        "dev:wildtrack": "pnpm --filter camera-trap-dashboard dev",
        "dev:fire": "pnpm --filter fire-app dev",
        "dev:trap": "pnpm --filter trap-monitor-dashboard dev"
      }
    }
    ```

    **File 3: .npmrc**

    ```ini
    node-linker=hoisted
    shamefully-hoist=true
    ```

    Reason for hoisted: Next.js 16 occasionally fails to resolve peer deps from nested workspace packages without flat node_modules. This is the safe default for Next.js monorepos.
  </action>
  <verify>
    <automated>
      cat "c:/Software code GITs/LandManagment Website/pnpm-workspace.yaml" | grep "catalog:" &&
      cat "c:/Software code GITs/LandManagment Website/package.json" | grep "trackline-workspace" &&
      cat "c:/Software code GITs/LandManagment Website/.npmrc" | grep "node-linker"
    </automated>
  </verify>
  <done>
    - pnpm-workspace.yaml exists and contains `catalog:` with 17 pinned entries
    - package.json exists with name `trackline-workspace` and 8 workspace scripts
    - .npmrc exists with `node-linker=hoisted`
  </done>
</task>

<task type="auto">
  <name>Task 2: Create packages/tsconfig and packages/eslint-config</name>
  <files>
    C:/Software code GITs/LandManagment Website/packages/tsconfig/package.json
    C:/Software code GITs/LandManagment Website/packages/tsconfig/tsconfig.base.json
    C:/Software code GITs/LandManagment Website/packages/eslint-config/package.json
    C:/Software code GITs/LandManagment Website/packages/eslint-config/index.mjs
  </files>
  <read_first>
    - Read `c:/Software code GITs/LandManagment Website/portal/tsconfig.json` to confirm compilerOptions that go into the base.
    - Read `c:/Software code GITs/LandManagment Website/portal/eslint.config.mjs` to confirm the exact content to extract into the shared package.
  </read_first>
  <action>
    Create the `packages/` directory with two internal packages.

    **File 1: packages/tsconfig/package.json**

    ```json
    {
      "name": "@trackline/tsconfig",
      "version": "0.0.1",
      "private": true,
      "exports": {
        "./base.json": "./tsconfig.base.json"
      }
    }
    ```

    **File 2: packages/tsconfig/tsconfig.base.json**

    Extract the shared compilerOptions. Omit `incremental`, `plugins`, and `paths` — these are
    app-specific and must remain in each app's tsconfig.json.

    ```json
    {
      "$schema": "https://json.schemastore.org/tsconfig",
      "compilerOptions": {
        "target": "ES2017",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "strict": true,
        "noEmit": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "react-jsx"
      }
    }
    ```

    **File 3: packages/eslint-config/package.json**

    Note: `eslint-config-next` is a peerDependency (not a direct dep). The consuming app already
    has it. Adding it as a direct dep here creates two plugin instances.

    ```json
    {
      "name": "@trackline/eslint-config",
      "version": "0.0.1",
      "private": true,
      "type": "module",
      "main": "./index.mjs",
      "exports": {
        ".": "./index.mjs"
      },
      "peerDependencies": {
        "eslint": "^9",
        "eslint-config-next": "*"
      }
    }
    ```

    **File 4: packages/eslint-config/index.mjs**

    ```js
    import { defineConfig, globalIgnores } from "eslint/config";
    import nextVitals from "eslint-config-next/core-web-vitals";
    import nextTs from "eslint-config-next/typescript";

    export const nextConfig = defineConfig([
      ...nextVitals,
      ...nextTs,
      globalIgnores([
        ".next/**",
        "out/**",
        "build/**",
        "next-env.d.ts",
      ]),
    ]);
    ```
  </action>
  <verify>
    <automated>
      cat "c:/Software code GITs/LandManagment Website/packages/tsconfig/tsconfig.base.json" | grep "\"strict\"" &&
      cat "c:/Software code GITs/LandManagment Website/packages/tsconfig/tsconfig.base.json" | grep -v "incremental" &&
      cat "c:/Software code GITs/LandManagment Website/packages/eslint-config/index.mjs" | grep "nextConfig" &&
      cat "c:/Software code GITs/LandManagment Website/packages/eslint-config/package.json" | grep "peerDependencies"
    </automated>
  </verify>
  <done>
    - packages/tsconfig/tsconfig.base.json has `"strict": true` and does NOT contain `incremental` or `plugins`
    - packages/eslint-config/index.mjs exports `nextConfig`
    - packages/eslint-config/package.json declares eslint-config-next as peerDependency, not dependency
    - Both package.json files have `@trackline/*` names
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `cat "c:/Software code GITs/LandManagment Website/pnpm-workspace.yaml"` — shows packages list with all 4 app paths and catalog with 17 entries
2. `cat "c:/Software code GITs/LandManagment Website/package.json"` — shows name: trackline-workspace with typecheck/lint/build/dev scripts
3. `cat "c:/Software code GITs/LandManagment Website/packages/tsconfig/tsconfig.base.json"` — strict: true present, no incremental/plugins/paths
4. `cat "c:/Software code GITs/LandManagment Website/packages/eslint-config/index.mjs"` — exports nextConfig
5. `ls "c:/Software code GITs/LandManagment Website/packages/"` — shows tsconfig/ and eslint-config/
</verification>

<success_criteria>
- Workspace root contains: pnpm-workspace.yaml, package.json, .npmrc
- packages/ directory contains: tsconfig/ and eslint-config/ with correct content
- No app files have been modified yet (those changes happen in Plan 03)
- The shared packages use peerDependencies correctly (no bundled copies of eslint-config-next)
</success_criteria>

<output>
After completion, create `.planning/phases/02-monorepo-workspace/02-02-SUMMARY.md` following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
