---
phase: 02-monorepo-workspace
plan: "04"
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - "C:/Software code GITs/LandManagment Website/pnpm-lock.yaml"
  - "C:/Software code GITs/LandManagment Website/portal/vercel.json"
  - "C:/Software code GITs/LandManagment Website/camera-trap-dashboard/vercel.json"
  - "C:/Software code GITs/LandManagment Website/fire-app/vercel.json"
  - "C:/Software code GITs/LandManagment Website/trap-monitor/frontend/vercel.json"
  - "C:/Software code GITs/LandManagment Website/camera-trap-dashboard/.planning/config.json"
  - "C:/Software code GITs/LandManagment Website/fire-app/.planning/config.json"
  - "C:/Software code GITs/LandManagment Website/trap-monitor/frontend/.planning/config.json"
  - "C:/Software code GITs/LandManagment Website/docs/CONVENTIONS.md"
autonomous: true
requirements:
  - MONO-01
  - MONO-04
  - CONV-03
  - CONV-04
must_haves:
  truths:
    - "pnpm install from workspace root succeeds and creates a single pnpm-lock.yaml"
    - "pnpm -r run typecheck from workspace root runs tsc across all 4 apps"
    - "pnpm -r run lint from workspace root runs eslint across all 4 apps"
    - "All 4 individual package-lock.json files are deleted"
    - "GSD .planning/config.json exists in all 3 satellite apps"
    - "CONVENTIONS.md documents TypeScript naming patterns"
    - "Each app has a vercel.json with workspace-aware install and build commands"
  artifacts:
    - path: "C:/Software code GITs/LandManagment Website/pnpm-lock.yaml"
      provides: "Single workspace lockfile"
    - path: "C:/Software code GITs/LandManagment Website/docs/CONVENTIONS.md"
      provides: "TypeScript naming convention reference"
      contains: "kebab-case"
    - path: "C:/Software code GITs/LandManagment Website/portal/vercel.json"
      provides: "Vercel workspace-aware deploy config for portal"
      contains: "pnpm install"
  key_links:
    - from: "pnpm-lock.yaml"
      to: "pnpm-workspace.yaml"
      via: "pnpm install from workspace root"
      pattern: "pnpm-lock.yaml exists at LandManagment Website/"
    - from: "vercel.json installCommand"
      to: "pnpm-workspace.yaml"
      via: "cd ../.. && pnpm install --frozen-lockfile"
      pattern: "pnpm install --frozen-lockfile"
---

<objective>
Run pnpm install to wire the workspace, delete per-app lockfiles, add vercel.json to each app for workspace-aware Vercel deploys, initialize GSD in the three satellite apps, and write the CONVENTIONS.md document.

Purpose: This plan validates that the workspace actually works (Plans 01-03 built infrastructure; this plan proves it runs). It also covers the remaining requirements: GSD init (CONV-03), naming conventions doc (CONV-04), and Vercel deploy compatibility.

Output: pnpm-lock.yaml, 4 vercel.json files, 3 .planning/config.json files, CONVENTIONS.md, package-lock.json files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-monorepo-workspace/02-03-SUMMARY.md
</context>

<interfaces>
<!-- Workspace root: C:/Software code GITs/LandManagment Website/ -->
<!-- App layout after Plans 01-03: -->
<!--   portal/             ← one level deep from workspace root -->
<!--   camera-trap-dashboard/  ← one level deep -->
<!--   fire-app/           ← one level deep -->
<!--   trap-monitor/frontend/  ← two levels deep (trap-monitor/ is git root) -->

<!-- vercel.json cd depth from each app dir to workspace root: -->
<!--   portal/ → cd .. (one up) -->
<!--   camera-trap-dashboard/ → cd .. (one up) -->
<!--   fire-app/ → cd .. (one up) -->
<!--   trap-monitor/frontend/ → cd ../.. (two up) -->

<!-- App package.json names (for --filter): -->
<!--   portal → "portal" -->
<!--   camera-trap-dashboard → "camera-trap-dashboard" -->
<!--   fire-app → "fire-app" -->
<!--   trap-monitor/frontend → "trap-monitor-dashboard" -->

<!-- GSD config.json minimal structure (from portal's .planning/): -->
<!-- {  "project": "...", "version": "1.0", "planner_model": "opus", -->
<!--    "checker_model": "sonnet", "researcher_model": "sonnet" } -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run pnpm install, delete package-lock.json files, create vercel.json files</name>
  <files>
    C:/Software code GITs/LandManagment Website/pnpm-lock.yaml
    C:/Software code GITs/LandManagment Website/portal/vercel.json
    C:/Software code GITs/LandManagment Website/camera-trap-dashboard/vercel.json
    C:/Software code GITs/LandManagment Website/fire-app/vercel.json
    C:/Software code GITs/LandManagment Website/trap-monitor/frontend/vercel.json
  </files>
  <read_first>
    - Check if pnpm is available: `pnpm --version` (must succeed — installed in Plan 01)
    - Check current workspace root: `ls "c:/Software code GITs/LandManagment Website/"` — must show pnpm-workspace.yaml, package.json, .npmrc, portal/, camera-trap-dashboard/, fire-app/, trap-monitor/
    - Check for existing vercel.json in any app: `ls "c:/Software code GITs/LandManagment Website/portal/vercel.json" 2>/dev/null`
    - Check existing package-lock.json files:
      `ls "c:/Software code GITs/LandManagment Website/portal/package-lock.json" 2>/dev/null`
      `ls "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/package-lock.json" 2>/dev/null`
  </read_first>
  <action>
    **Step 1 — Run pnpm install from workspace root**

    ```bash
    cd "c:/Software code GITs/LandManagment Website" && pnpm install
    ```

    Expected outcome: pnpm resolves all workspace packages, creates `pnpm-lock.yaml` at the workspace root, and symlinks @trackline/tsconfig and @trackline/eslint-config into each app's node_modules.

    If pnpm install fails with a resolution error for `@trackline/tsconfig` or `@trackline/eslint-config`:
    - Verify `packages/tsconfig/package.json` has `"name": "@trackline/tsconfig"` (check it)
    - Verify `packages/eslint-config/package.json` has `"name": "@trackline/eslint-config"`
    - Verify `pnpm-workspace.yaml` has `'packages/*'` in the packages list

    If pnpm install fails with a catalog resolution error:
    - Check that the dep key in an app's package.json exactly matches the key in pnpm-workspace.yaml catalog
    - For example, `"@supabase/ssr": "catalog:"` in package.json requires `'@supabase/ssr': '^0.9.0'` in catalog

    **Step 2 — Delete per-app package-lock.json files**

    ```bash
    rm -f "c:/Software code GITs/LandManagment Website/portal/package-lock.json"
    rm -f "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/package-lock.json"
    rm -f "c:/Software code GITs/LandManagment Website/fire-app/package-lock.json"
    rm -f "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/package-lock.json"
    ```

    **Step 3 — Create vercel.json for each app**

    Each app needs a vercel.json so Vercel runs pnpm install from the workspace root and then builds the specific app. The `cd` depth depends on how deep the app is from the workspace root.

    Apps one level deep (portal, camera-trap-dashboard, fire-app):
    ```json
    {
      "installCommand": "cd .. && pnpm install --frozen-lockfile",
      "buildCommand": "cd .. && pnpm --filter <package-name> build"
    }
    ```

    trap-monitor/frontend is two levels deep (trap-monitor/ then frontend/):
    ```json
    {
      "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
      "buildCommand": "cd ../.. && pnpm --filter trap-monitor-dashboard build"
    }
    ```

    Create these exact files:

    **portal/vercel.json:**
    ```json
    {
      "installCommand": "cd .. && pnpm install --frozen-lockfile",
      "buildCommand": "cd .. && pnpm --filter portal build"
    }
    ```

    **camera-trap-dashboard/vercel.json:**
    ```json
    {
      "installCommand": "cd .. && pnpm install --frozen-lockfile",
      "buildCommand": "cd .. && pnpm --filter camera-trap-dashboard build"
    }
    ```

    **fire-app/vercel.json:**
    ```json
    {
      "installCommand": "cd .. && pnpm install --frozen-lockfile",
      "buildCommand": "cd .. && pnpm --filter fire-app build"
    }
    ```

    **trap-monitor/frontend/vercel.json:**
    ```json
    {
      "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
      "buildCommand": "cd ../.. && pnpm --filter trap-monitor-dashboard build"
    }
    ```

    NOTE: If portal/vercel.json already exists, read it first and preserve any existing settings
    that are not install/build commands (e.g., headers, rewrites, env vars).
  </action>
  <verify>
    <automated>
      ls "c:/Software code GITs/LandManagment Website/pnpm-lock.yaml" &&
      ls "c:/Software code GITs/LandManagment Website/portal/vercel.json" &&
      ls "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/vercel.json" &&
      ls "c:/Software code GITs/LandManagment Website/fire-app/vercel.json" &&
      ls "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/vercel.json" &&
      test ! -f "c:/Software code GITs/LandManagment Website/portal/package-lock.json" &&
      test ! -f "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/package-lock.json"
    </automated>
  </verify>
  <done>
    - pnpm-lock.yaml exists at workspace root (pnpm install succeeded)
    - All 4 vercel.json files exist with workspace-aware install commands
    - portal/package-lock.json and camera-trap-dashboard/package-lock.json are gone
  </done>
</task>

<task type="auto">
  <name>Task 2: GSD init satellite apps and create CONVENTIONS.md</name>
  <files>
    C:/Software code GITs/LandManagment Website/camera-trap-dashboard/.planning/config.json
    C:/Software code GITs/LandManagment Website/fire-app/.planning/config.json
    C:/Software code GITs/LandManagment Website/trap-monitor/frontend/.planning/config.json
    C:/Software code GITs/LandManagment Website/docs/CONVENTIONS.md
  </files>
  <read_first>
    - Read the portal .planning/config.json to understand GSD config structure:
      `cat "c:/Software code GITs/LandManagment Website/portal/.planning/config.json" 2>/dev/null || echo "not found"`
    - Check if any satellite apps already have .planning/:
      `ls "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/.planning/" 2>/dev/null`
      `ls "c:/Software code GITs/LandManagment Website/fire-app/.planning/" 2>/dev/null`
      `ls "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/.planning/" 2>/dev/null`
    - Check if docs/ directory exists at workspace root:
      `ls "c:/Software code GITs/LandManagment Website/docs/" 2>/dev/null`
  </read_first>
  <action>
    **Part A — GSD .planning/config.json for each satellite app**

    Create a minimal `.planning/config.json` in each satellite app. These satellite apps do not
    need a full GSD project setup (no ROADMAP.md, REQUIREMENTS.md) — just the config file so
    GSD tools (`gsd-tools.cjs`) can run in those directories.

    The config structure mirrors what portal has. Read portal's config.json and use the same
    field names. A safe minimal config:

    ```json
    {
      "version": "1.0",
      "planner_model": "opus",
      "checker_model": "sonnet",
      "researcher_model": "sonnet",
      "research_enabled": true,
      "plan_checker_enabled": true,
      "commit_docs": true,
      "text_mode": false
    }
    ```

    Create this file at:
    - `c:/Software code GITs/LandManagment Website/camera-trap-dashboard/.planning/config.json`
    - `c:/Software code GITs/LandManagment Website/fire-app/.planning/config.json`
    - `c:/Software code GITs/LandManagment Website/trap-monitor/frontend/.planning/config.json`

    If the directory `.planning/` does not exist in an app, create it first.

    **Part B — docs/CONVENTIONS.md**

    Create `C:/Software code GITs/LandManagment Website/docs/CONVENTIONS.md`.
    Create the `docs/` directory if it does not exist.

    This document captures TypeScript and code conventions that are already present in the
    codebase (observed patterns, not new rules). Content:

    ```markdown
    # Trackline — Code Conventions

    Applies to all apps: portal, WildTrack (camera-trap-dashboard), Fire System (fire-app), Trap Monitor (trap-monitor).

    ## File Naming

    - **Source files:** kebab-case — `check-access.ts`, `app-switcher.tsx`, `use-auth.ts`
    - **Test files:** same stem with `.test.ts` or `.test.tsx` suffix — `check-access.test.ts`
    - **Page/layout files:** Next.js conventions — `page.tsx`, `layout.tsx`, `loading.tsx`

    ## TypeScript Naming

    | Entity | Convention | Examples |
    |--------|------------|---------|
    | Components | PascalCase | `AppSwitcher`, `UserAvatar`, `StatusBadge` |
    | Hooks | camelCase with `use` prefix | `useAuth`, `useAppAccess` |
    | Functions | camelCase | `checkAppAccess`, `getUserApps`, `formatDate` |
    | Variables | camelCase | `currentUser`, `appList`, `isLoading` |
    | Types / Interfaces | PascalCase, no `I` prefix | `AppAccess`, `UserProfile`, `SessionToken` |
    | Enums | PascalCase | `AppStatus`, `UserRole` |
    | Module-level constants | SCREAMING_SNAKE_CASE | `MAX_RETRY_COUNT`, `DEFAULT_TIMEOUT` |
    | Local constants | camelCase | `defaultColor`, `maxItems` |

    ## Export Rules

    - **Named exports everywhere** — `export function`, `export type`, `export const`
    - **Default exports only for:** Next.js page components (`page.tsx`, `layout.tsx`), root `error.tsx`
    - Never use `export default` for utility functions, hooks, types, or shared components

    ## React / Next.js Conventions

    - **Server Components by default** — no `"use client"` unless event handlers or React hooks are required
    - **`"use client"` triggers:** `useState`, `useEffect`, `useCallback`, `useRef`, onClick, onChange
    - **API routes:** `export async function GET/POST/PUT/DELETE` (App Router convention)
    - **Server Actions:** file-level `"use server"` directive, not component-level

    ## Supabase Conventions

    - Client factory: `createClient()` from `@/lib/supabase/client` (browser) or `@/lib/supabase/server` (server)
    - Never import `createBrowserClient` or `createServerClient` directly — always use the app's factory
    - RLS enforced on all portal-schema tables — never bypass with `service_role` key in app code

    ## Import Order

    1. External packages (`react`, `next/*`, `@supabase/*`)
    2. Internal workspace packages (`@trackline/*`)
    3. App-internal aliases (`@/components/*`, `@/lib/*`)
    4. Relative imports (`./`, `../`)

    ## TypeScript Strictness

    All apps extend `@trackline/tsconfig/base.json` which enables `"strict": true`. This means:
    - No implicit `any`
    - No unchecked optional chaining without null checks
    - All function parameters typed explicitly
    - Return types explicit on exported functions

    ## Adding New Shared Dependencies

    1. Add the dep to the catalog in `pnpm-workspace.yaml` at workspace root
    2. In the consuming app's `package.json`, use `"dep-name": "catalog:"`
    3. Run `pnpm install` from workspace root (never from inside an app directory)
    ```

    Write the file at: `C:/Software code GITs/LandManagment Website/docs/CONVENTIONS.md`
  </action>
  <verify>
    <automated>
      ls "c:/Software code GITs/LandManagment Website/camera-trap-dashboard/.planning/config.json" &&
      ls "c:/Software code GITs/LandManagment Website/fire-app/.planning/config.json" &&
      ls "c:/Software code GITs/LandManagment Website/trap-monitor/frontend/.planning/config.json" &&
      grep "kebab-case" "c:/Software code GITs/LandManagment Website/docs/CONVENTIONS.md"
    </automated>
  </verify>
  <done>
    - .planning/config.json exists in all 3 satellite app directories
    - docs/CONVENTIONS.md exists at workspace root and contains naming convention tables
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify workspace-wide typecheck and lint run</name>
  <files>N/A — verification only</files>
  <read_first>
    - Confirm pnpm-lock.yaml exists: `ls "c:/Software code GITs/LandManagment Website/pnpm-lock.yaml"`
    - Confirm @trackline packages are symlinked: `ls "c:/Software code GITs/LandManagment Website/portal/node_modules/@trackline/" 2>/dev/null`
  </read_first>
  <action>
    Run workspace-wide verification commands from the workspace root.

    ```bash
    cd "c:/Software code GITs/LandManagment Website" && pnpm -r run typecheck
    ```

    Expected: tsc --noEmit runs in all 4 apps. TypeScript errors may exist (pre-existing app issues)
    but pnpm itself must not fail to invoke the command.

    If typecheck fails because an app does not have a typecheck script:
    - Add `"typecheck": "tsc --noEmit"` to that app's package.json scripts
    - Re-run pnpm install from workspace root

    ```bash
    cd "c:/Software code GITs/LandManagment Website" && pnpm -r run lint
    ```

    Expected: ESLint runs in all 4 apps using the shared flat config. Lint errors may exist
    (pre-existing) but the command must not fail with "Config file not found" or module resolution
    errors for @trackline/eslint-config.

    If lint fails with module resolution error for @trackline/eslint-config:
    - Verify @trackline/eslint-config is in node_modules: `ls portal/node_modules/@trackline/`
    - If missing, re-run `pnpm install` from workspace root

    Document any pre-existing TypeScript or lint errors found in the summary — these are known
    debt to address in later phases, not blockers for Phase 2 completion.
  </action>
  <verify>
    <automated>
      cd "c:/Software code GITs/LandManagment Website" && pnpm --filter portal run typecheck 2>&1 | grep -v "error TS" | tail -5
    </automated>
  </verify>
  <done>
    - `pnpm -r run typecheck` runs tsc in all 4 apps (exit codes may include non-zero if pre-existing TS errors exist, but pnpm resolves and invokes the command in all 4)
    - `pnpm -r run lint` runs eslint in all 4 apps without "module not found" errors for @trackline packages
    - Any pre-existing errors are documented in the summary (not treated as Phase 2 failures)
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run these checks from the workspace root (`C:/Software code GITs/LandManagment Website/`):

1. `ls pnpm-lock.yaml` — workspace lockfile exists
2. `pnpm --filter portal run typecheck` — portal typecheck invokes
3. `pnpm --filter portal run lint` — portal lint invokes with shared config
4. `ls portal/node_modules/@trackline/tsconfig` — workspace package is symlinked
5. `ls portal/node_modules/@trackline/eslint-config` — workspace package is symlinked
6. `ls docs/CONVENTIONS.md` — conventions document exists
7. `ls camera-trap-dashboard/.planning/config.json` — GSD initialized
8. `ls fire-app/.planning/config.json` — GSD initialized
9. `ls trap-monitor/frontend/.planning/config.json` — GSD initialized
</verification>

<success_criteria>
- pnpm install from workspace root succeeds: single pnpm-lock.yaml, no per-app node_modules conflicts
- pnpm -r run typecheck runs tsc in all 4 apps
- pnpm -r run lint runs eslint in all 4 apps using shared flat config
- No package-lock.json files remain in any of the 4 apps
- Each app has vercel.json with workspace-aware install + build commands
- GSD .planning/config.json in all 3 satellite apps
- docs/CONVENTIONS.md documents the established naming patterns
</success_criteria>

<output>
After completion, create `.planning/phases/02-monorepo-workspace/02-04-SUMMARY.md` following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md
</output>
