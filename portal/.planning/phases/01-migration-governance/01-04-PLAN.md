---
phase: 01-migration-governance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md
  - c:/Software code GITs/camera-trap-dashboard/CLAUDE.md
  - c:/Software code GITs/Fire project system/fire-app/CLAUDE.md
  - CLAUDE.md
  - src/lib/check-access.ts
  - c:/Software code GITs/camera-trap-dashboard/src/lib/check-access.ts
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - SEC-01

must_haves:
  truths:
    - "CLAUDE.md exists in all four repos (portal, WildTrack, Fire App, Trap Monitor)"
    - "All four CLAUDE.md files contain the Supabase safety rules section referencing PROTECTED_SURFACES.md"
    - "The bootstrap fallback (hasAccess: true, role: 'admin') is absent from production builds in portal and WildTrack"
    - "In development (NODE_ENV !== 'production'), the bootstrap fallback still works for local development workflow"
    - "Fire App and Trap Monitor check-access.ts are already fail-closed — no changes needed"
  artifacts:
    - path: "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"
      provides: "AI agent rules for Trap Monitor — prevents accidental shared surface modifications"
      min_lines: 40
    - path: "src/lib/check-access.ts"
      provides: "Portal check-access with SEC-01 fix applied"
      contains: "NODE_ENV !== 'production'"
    - path: "c:/Software code GITs/camera-trap-dashboard/src/lib/check-access.ts"
      provides: "WildTrack check-access with SEC-01 fix applied"
      contains: "NODE_ENV !== 'production'"
  key_links:
    - from: "src/lib/check-access.ts"
      to: "process.env.NODE_ENV"
      via: "Bootstrap fallback gated by NODE_ENV !== 'production'"
      pattern: "NODE_ENV !== 'production'"
    - from: "c:/Software code GITs/camera-trap-dashboard/src/lib/check-access.ts"
      to: "process.env.NODE_ENV"
      via: "Bootstrap fallback gated by NODE_ENV !== 'production'"
      pattern: "NODE_ENV !== 'production'"
---

<objective>
Propagate CLAUDE.md to all four repos (creating Trap Monitor's for the first time, updating all with shared Supabase safety rules) and apply the SEC-01 security fix that gates the bootstrap fallback behind NODE_ENV in portal and WildTrack check-access.ts.

Purpose: Ensures AI agents working in any Trackline repo know the cross-app safety rules from day one. Closes a production security hole where portal schema failures grant admin access to all users.

Output:
- Trap Monitor/frontend/CLAUDE.md (new)
- Updated CLAUDE.md sections in WildTrack and Fire App (Supabase safety rules section added/updated)
- Portal CLAUDE.md updated (ensure safety rules section is complete — it is the template)
- src/lib/check-access.ts (SEC-01 fix: portal)
- camera-trap-dashboard/src/lib/check-access.ts (SEC-01 fix: WildTrack)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-migration-governance/01-RESEARCH.md
</context>

<interfaces>
<!-- Current check-access.ts bootstrap fallback (BEFORE fix) — portal and WildTrack: -->
<!--
  // Local bootstrap fallback: allow signed-in users while portal schema/RPC is being provisioned.
  if (accessInfraMissing) {
    return { hasAccess: true, role: "admin" };
  }
-->

<!-- Required replacement (AFTER fix — SEC-01): -->
<!--
  if (accessInfraMissing) {
    if (process.env.NODE_ENV !== 'production') {
      // Development only: bootstrap while portal schema is being provisioned.
      // This branch MUST NOT execute in production. Verified: Next.js sets
      // NODE_ENV=production for `next build` and `next start`.
      // Safe context: called from Server Components only — not Edge Runtime.
      return { hasAccess: true, role: "admin" };
    }
    // Production: fail closed. Portal must be reachable.
    console.warn(JSON.stringify({
      level: 'warn',
      msg: 'portal_access_check_unavailable',
      app_id: appId,
      error: errorMessage,
      ts: new Date().toISOString(),
    }));
    return { hasAccess: false, role: null };
  }
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Trap Monitor CLAUDE.md and update all repos' safety rules</name>
  <files>
    c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md
    c:/Software code GITs/camera-trap-dashboard/CLAUDE.md
    c:/Software code GITs/Fire project system/fire-app/CLAUDE.md
    CLAUDE.md
  </files>
  <read_first>
    - CLAUDE.md (portal — read in full; this is the template and must remain the most complete version)
    - c:/Software code GITs/camera-trap-dashboard/CLAUDE.md (read in full before updating)
    - c:/Software code GITs/Fire project system/fire-app/CLAUDE.md (read in full before updating)
    - .planning/phases/01-migration-governance/01-RESEARCH.md (Pitfall 4: Trap Monitor Has No CLAUDE.md — content requirements; and CONV-01/CONV-02 requirement descriptions)
  </read_first>
  <action>
    Step 1: Create c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md

    Use portal/CLAUDE.md as the structural template. Adapt the content for Trap Monitor:

    # Project AI Instructions — Trap Monitor

    This file defines project-specific development rules for all AI agents (Claude Code, GitHub Copilot).

    Global behaviour rules come from:
    `C:\Users\tobyw\AI Global rules\rules\GLOBAL_AI_CODING_RULES.md`

    # Critical Security Rules

    Never:
    - hardcode API keys, tokens, or secrets
    - commit `.env` or credential files
    - expose tokens to client-side code
    - run destructive commands without explicit approval
    - call external APIs over HTTP — HTTPS only

    If a secret appears in code, stop and warn the user immediately.

    # Project Stack

    - **Framework:** Next.js (App Router)
    - **Language:** TypeScript (strict)
    - **Database & Auth:** Supabase (shared project across all Trackline apps)
    - **SMS Integration:** Twilio (inbound/outbound SMS for trap hardware)
    - **Deployment:** Vercel

    # Architecture

    This is the **Trap Monitor** app in the Trackline conservation technology suite. It monitors SMS-connected hardware traps and notifies users of trap events.

    ## Related Apps (same Supabase project)
    - **Portal** — authentication and access control (`LandManagment Website/portal/`)
    - **WildTrack** — camera trap management (`camera-trap-dashboard/`)
    - **Fire System** — fire management & carbon (`Fire project system/fire-app/`)

    # Shared Database Rules — CRITICAL

    All Trackline apps share **one Supabase project**. Careless changes here break the Portal, WildTrack, and Fire System.

    ## Trap Monitor Owns (this project may CREATE, ALTER, DROP)

    - **Tables (public schema):** `units`, `events`, `commands`, `notifications`
    - **Functions (public schema):** `trap_can_view_org()`, `trap_can_edit_org()`, `trap_can_admin_org()`

    ## Shared Infrastructure Trap Monitor Depends On

    - **`portal.app_access`** — Supabase RPC `check_app_access()` called on every page load for access gating
    - **`portal.profiles`** — User profile data (display_name, email)
    - **`public.organisations`** — OWNED BY WildTrack. Trap Monitor units have org_id FK to this table. Do NOT ALTER or DROP organisations columns without checking Trap Monitor FK dependencies.
    - **`public.org_members`** — OWNED BY WildTrack. `trap_can_*` functions query this table for permission checks. Do NOT DROP or rename columns without checking trap_can_* functions.
    - **`public.is_org_member(p_org_id uuid, p_user_id uuid)`** — OWNED BY WildTrack. Used in Trap Monitor RLS policies. Signature must not change.
    - **`public.is_org_admin(p_org_id uuid, p_user_id uuid)`** — OWNED BY WildTrack. Used in Trap Monitor RLS policies.
    - **`public.can_org_edit(p_org_id uuid, p_user_id uuid)`** — OWNED BY WildTrack. Used in Trap Monitor RLS policies.

    ## Never Touch from This Project

    - `portal.*` tables and functions (Portal owns the portal schema)
    - `public.organisations` — owned by WildTrack
    - `public.org_members` — owned by WildTrack
    - `public.is_org_member`, `public.is_org_admin`, `public.can_org_edit` — owned by WildTrack
    - Any Fire App tables (organization, project, user_project, burn_plan, fire_season, fire_scar, etc.)

    ## Governance Gap — No Migration Files

    **IMPORTANT:** Trap Monitor has no `supabase/migrations/` directory. The schema was applied via the Supabase dashboard. This means:
    1. Schema history is not version-controlled
    2. Any future schema change MUST create a `supabase/migrations/` directory and use the `trap_` naming convention: `trap_001_description.sql`
    3. See `portal/docs/schema/trap-monitor.md` for the reconstructed schema reference
    4. See `portal/PROTECTED_SURFACES.md` for the full cross-app surface inventory

    ## Rules for Schema Changes

    1. Never DROP or ALTER tables/functions outside Trap Monitor-owned surfaces without explicit user approval
    2. Never modify `trap_can_*` functions without confirming the WildTrack helper functions they depend on are unchanged
    3. If adding columns to Trap Monitor tables, use `ALTER TABLE ... ADD COLUMN ... DEFAULT` to avoid breaking existing queries
    4. New Trap Monitor tables must have RLS enabled and grants for the `authenticated` role
    5. Run `node portal/scripts/db-check.cjs <migration.sql>` before applying any migration that touches portal surfaces

    ## Cross-App Impact Checklist

    Before any migration change, verify:
    - [ ] Does this affect `public.organisations` or `public.org_members`? → WildTrack breaks
    - [ ] Does this change `trap_can_*` function signatures? → Trap Monitor RLS policies break
    - [ ] Does this affect `portal.app_access`? → Access gating breaks for all apps
    - [ ] Does this change the `units.org_id` FK relationship? → WildTrack org association breaks

    # Reporting

    When work is complete provide:
    - what changed
    - why it changed
    - files modified
    - verification performed
    - any risks or assumptions

    ---

    Step 2: Update camera-trap-dashboard/CLAUDE.md

    Read the existing file first. Add or update the "Shared Database Rules" section to ensure it includes:
    - Reference to `portal/PROTECTED_SURFACES.md` as the authoritative cross-app surface inventory
    - Reference to `node portal/scripts/db-check.cjs <migration.sql>` as the pre-migration check command
    - Explicit note that WildTrack-owned helper functions (is_org_member, is_org_admin, can_org_edit) are also used by Trap Monitor RLS — signature changes break Trap Monitor
    - Explicit note that public.organisations and public.org_members changes affect Trap Monitor FK constraints

    Do NOT rewrite the entire file. Locate the "Shared Database Rules" or equivalent section and add/update the missing items only.

    Step 3: Update Fire project system/fire-app/CLAUDE.md

    Read the existing file first. Add or update the "Shared Database Rules" section to ensure it includes:
    - Reference to `portal/PROTECTED_SURFACES.md`
    - Reference to `node portal/scripts/db-check.cjs`
    - Note about the public.update_updated_at() naming collision risk with WildTrack (both define this function; CREATE OR REPLACE overwrites)

    Do NOT rewrite the entire file.

    Step 4: Update portal/CLAUDE.md (if needed)

    Read the existing portal CLAUDE.md. Verify the "Shared Database Rules" section already references PROTECTED_SURFACES.md and scripts/db-check.cjs. If these references are missing, add them to the existing section. Otherwise, no changes needed.
  </action>
  <verify>
    <automated>
      # Trap Monitor CLAUDE.md exists
      test -f "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md" && echo "Trap Monitor CLAUDE.md OK"
      wc -l "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"

      # Key content checks
      grep "PROTECTED_SURFACES.md" "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"
      grep "public.organisations" "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"
      grep "trap_can_" "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"
      grep "db-check.cjs" "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"

      # Other repos reference db-check
      grep "db-check.cjs" "c:/Software code GITs/camera-trap-dashboard/CLAUDE.md"
      grep "db-check.cjs" "c:/Software code GITs/Fire project system/fire-app/CLAUDE.md"

      # Naming collision warning in fire app
      grep "update_updated_at" "c:/Software code GITs/Fire project system/fire-app/CLAUDE.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"` exits 0
    - Trap Monitor CLAUDE.md has at least 40 lines
    - grep "PROTECTED_SURFACES.md" in Trap Monitor CLAUDE.md returns at least one match
    - grep "public.organisations" in Trap Monitor CLAUDE.md returns at least one match documenting WildTrack ownership
    - grep "db-check.cjs" in camera-trap-dashboard/CLAUDE.md returns at least one match
    - grep "db-check.cjs" in fire-app/CLAUDE.md returns at least one match
    - grep "Governance Gap" in Trap Monitor CLAUDE.md returns at least one match
  </acceptance_criteria>
  <done>CLAUDE.md exists in all four repos. Trap Monitor has a new CLAUDE.md with project-specific rules and cross-app safety documentation. WildTrack and Fire App CLAUDE.md files reference PROTECTED_SURFACES.md and db-check.cjs. All four repos are ready for AI agent work without risk of accidental shared surface modification.</done>
</task>

<task type="auto">
  <name>Task 2: Apply SEC-01 fix to portal and WildTrack check-access.ts</name>
  <files>
    src/lib/check-access.ts
    c:/Software code GITs/camera-trap-dashboard/src/lib/check-access.ts
  </files>
  <read_first>
    - src/lib/check-access.ts (FULL FILE — read before any edit)
    - c:/Software code GITs/camera-trap-dashboard/src/lib/check-access.ts (FULL FILE — read before any edit)
    - .planning/phases/01-migration-governance/01-RESEARCH.md (Pattern 3: SEC-01 Bootstrap Fallback Gated by NODE_ENV — exact fix code, pitfall 3 about Edge Runtime)
  </read_first>
  <action>
    Apply the SEC-01 fix to the `checkAppAccess` function in both portal and WildTrack.

    Both files currently contain this block (lines 33–36):
    ```
    // Local bootstrap fallback: allow signed-in users while portal schema/RPC is being provisioned.
    if (accessInfraMissing) {
      return { hasAccess: true, role: "admin" };
    }
    ```

    Replace this block in BOTH files with:
    ```typescript
    if (accessInfraMissing) {
      if (process.env.NODE_ENV !== 'production') {
        // Development only: bootstrap while portal schema is being provisioned.
        // This branch MUST NOT execute in production. Verified: Next.js sets
        // NODE_ENV=production for `next build` and `next start`.
        // Safe context: called from Server Components only — not Edge Runtime.
        return { hasAccess: true, role: "admin" };
      }
      // Production: fail closed. Portal must be reachable.
      console.warn(JSON.stringify({
        level: 'warn',
        msg: 'portal_access_check_unavailable',
        app_id: appId,
        error: errorMessage,
        ts: new Date().toISOString(),
      }));
      return { hasAccess: false, role: null };
    }
    ```

    Replace the comment on the line before the block (currently "Local bootstrap fallback: allow signed-in users while portal schema/RPC is being provisioned.") with:
    "SEC-01: Bootstrap fallback gated by NODE_ENV. In production, fail closed."

    IMPORTANT — variable name check: The current code uses `appId` as the parameter name in `checkAppAccess(supabase, appId)`. Verify this matches before using it in the console.warn. If the parameter is named differently, use the actual parameter name.

    Do NOT modify any other part of either file. The fix is a targeted 4-line replacement of the `if (accessInfraMissing)` block.

    After the fix:
    - Verify with TypeScript that the changes compile: `cd "c:/Software code GITs/LandManagment Website/portal" && npx tsc --noEmit`
    - Also verify the WildTrack file: `cd "c:/Software code GITs/camera-trap-dashboard" && npx tsc --noEmit`

    Fire App and Trap Monitor: Both are already fail-closed (confirmed in RESEARCH.md). Do NOT touch their check-access.ts files.
  </action>
  <verify>
    <automated>
      # Verify fix applied in portal
      grep "NODE_ENV !== 'production'" "c:/Software code GITs/LandManagment Website/portal/src/lib/check-access.ts"

      # Verify old unconditional fallback is gone
      grep -c "return { hasAccess: true, role: \"admin\" }" "c:/Software code GITs/LandManagment Website/portal/src/lib/check-access.ts"
      # (should return 1 — inside the NODE_ENV check — not 0)

      # Verify the production fail-closed path exists
      grep "portal_access_check_unavailable" "c:/Software code GITs/LandManagment Website/portal/src/lib/check-access.ts"
      grep "hasAccess: false, role: null" "c:/Software code GITs/LandManagment Website/portal/src/lib/check-access.ts"

      # Same checks for WildTrack
      grep "NODE_ENV !== 'production'" "c:/Software code GITs/camera-trap-dashboard/src/lib/check-access.ts"
      grep "portal_access_check_unavailable" "c:/Software code GITs/camera-trap-dashboard/src/lib/check-access.ts"

      # TypeScript compile check (portal)
      cd "c:/Software code GITs/LandManagment Website/portal" && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "NODE_ENV !== 'production'" in portal src/lib/check-access.ts returns exactly 1 match
    - grep "NODE_ENV !== 'production'" in wildtrack src/lib/check-access.ts returns exactly 1 match
    - grep "portal_access_check_unavailable" in portal src/lib/check-access.ts returns exactly 1 match
    - grep "portal_access_check_unavailable" in wildtrack src/lib/check-access.ts returns exactly 1 match
    - `npx tsc --noEmit` in portal exits 0 (no TypeScript errors)
    - The old unconditional `return { hasAccess: true, role: "admin" }` still exists (inside the NODE_ENV check) — function is still usable in development
    - Fire App and Trap Monitor check-access.ts are UNCHANGED (verify by checking their last-modified timestamps or reading their accessInfraMissing block)
  </acceptance_criteria>
  <done>Portal and WildTrack checkAppAccess() fail closed in production when portal schema is unreachable. Development workflow is unaffected. TypeScript compiles cleanly. Fire App and Trap Monitor are unchanged (already fail-closed).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `test -f "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"` exits 0
2. `grep "NODE_ENV !== 'production'" src/lib/check-access.ts` returns 1 match
3. `grep "NODE_ENV !== 'production'" "c:/Software code GITs/camera-trap-dashboard/src/lib/check-access.ts"` returns 1 match
4. `grep "PROTECTED_SURFACES.md" "c:/Software code GITs/Trap Monitor/frontend/CLAUDE.md"` returns a match
5. Portal TypeScript compiles: `cd portal && npx tsc --noEmit` exits 0
6. WildTrack TypeScript compiles: `cd camera-trap-dashboard && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- All four repos have CLAUDE.md with Supabase safety rules — AI agents working in any repo know the cross-app rules
- Trap Monitor CLAUDE.md explicitly documents the WildTrack-owned shared surfaces and the Trap Monitor governance gap
- Bootstrap fallback is absent from production code paths in portal and WildTrack
- SEC-01: `NODE_ENV=production` environments never grant hasAccess: true due to portal schema unavailability
- TypeScript compiles cleanly in both modified repos
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-governance/01-04-SUMMARY.md`
</output>
