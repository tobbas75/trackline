---
phase: 04-shared-ui-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/tokens.css
  - packages/ui/src/index.ts
autonomous: true
requirements:
  - UI-01
  - UI-05

must_haves:
  truths:
    - "packages/ui/tokens.css exists and defines all Trackline CSS custom properties"
    - "packages/ui/src/index.ts is the package entry point with named exports"
    - "The package resolves in the workspace (pnpm shows @trackline/ui linked)"
    - "All colour, spacing, and animation tokens match the exact values in portal/src/app/globals.css"
  artifacts:
    - path: "packages/ui/package.json"
      provides: "@trackline/ui package manifest"
      contains: '"name": "@trackline/ui"'
    - path: "packages/ui/tokens.css"
      provides: "All design token CSS custom properties"
      contains: "--red-dust: #b5452a"
    - path: "packages/ui/src/index.ts"
      provides: "Package entry point"
  key_links:
    - from: "packages/ui/package.json"
      to: "packages/ui/src/index.ts"
      via: "main + exports fields"
      pattern: '"main".*index\.ts'
    - from: "packages/ui/tokens.css"
      to: "@theme inline block"
      via: "CSS custom properties mirroring portal globals.css"
      pattern: "--color-red-dust"
---

<objective>
Scaffold the @trackline/ui package with the full design token CSS file.

Purpose: Establish the single source of brand tokens (red-dust, ochre, eucalypt, sky, stone scale, typography aliases, utility classes) so components and consuming apps never redefine these values.

Output: packages/ui/ package — package.json, tsconfig.json, tokens.css (full token set), src/index.ts (entry point stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing package pattern to mirror — from packages/supabase-config/package.json -->
```json
{
  "name": "@trackline/supabase-config",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" }
}
```

<!-- Exact token values from portal/src/app/globals.css — these are the canonical values -->
:root {
  --stone-50: #faf8f6;  --stone-100: #f5f0eb;  --stone-200: #e8e0d8;
  --stone-300: #d4c8bc; --stone-400: #8a8580;  --stone-500: #6b5e54;
  --stone-600: #5a4e44; --stone-700: #3d3530;  --stone-800: #2a2320;
  --stone-900: #1a1412;
  --red-dust: #b5452a;  --red-dust-light: #d4664a;
  --ochre: #c9913a;     --ochre-light: #daa94e;
  --eucalypt: #4a7c59;  --eucalypt-light: #5d9b6f;
  --sky: #5b8fa8;
  --background: var(--stone-50);  --foreground: var(--stone-900);
  --muted: var(--stone-200);      --muted-foreground: var(--stone-400);
  --accent: var(--red-dust);      --accent-light: var(--red-dust-light);
}

<!-- pnpm catalog versions (pnpm-workspace.yaml) -->
tailwindcss: '^4'
typescript: '^5'
clsx: '^2.1.1'
tailwind-merge: '^3.5.0'
lucide-react: '^0.577.0'
react: '19.2.3'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/ui package manifest and tsconfig</name>
  <files>
    packages/ui/package.json
    packages/ui/tsconfig.json
    packages/ui/src/index.ts
  </files>
  <read_first>
    - packages/supabase-config/package.json (pattern to mirror)
    - packages/tsconfig/tsconfig.base.json (base to extend)
    - pnpm-workspace.yaml (catalog: versions)
  </read_first>
  <action>
Create packages/ui/package.json:
```json
{
  "name": "@trackline/ui",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./tokens.css"
  },
  "peerDependencies": {
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "dependencies": {
    "clsx": "catalog:",
    "lucide-react": "catalog:",
    "tailwind-merge": "catalog:"
  },
  "devDependencies": {
    "@trackline/tsconfig": "workspace:*",
    "typescript": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:"
  }
}
```

Create packages/ui/tsconfig.json:
```json
{
  "extends": "@trackline/tsconfig/tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
```

Create packages/ui/src/index.ts (stub for now — components added in Plan 02):
```ts
// Components — added in phase 04-02
// export { Button } from "./button";
// export { Card, CardHeader, CardBody } from "./card";
// export { Badge } from "./badge";
// export { Avatar } from "./avatar";

// Re-export cn utility
export { cn } from "./cn";
```

Create packages/ui/src/cn.ts:
```ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
  </action>
  <verify>
    <automated>cd "c:/Software code GITs/LandManagment Website" && pnpm install --filter @trackline/ui 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/package.json has name "@trackline/ui" and exports map with ".": "./src/index.ts" and "./tokens.css": "./tokens.css"
    - packages/ui/src/cn.ts exports cn function using clsx + twMerge
    - grep -r '"@trackline/ui"' packages/ui/package.json returns a match
  </acceptance_criteria>
  <done>Package scaffolded and resolvable in workspace — pnpm install succeeds for @trackline/ui</done>
</task>

<task type="auto">
  <name>Task 2: Write tokens.css with the full Trackline design token set</name>
  <files>
    packages/ui/tokens.css
  </files>
  <read_first>
    - portal/src/app/globals.css (canonical token values — copy exactly, do not invent values)
  </read_first>
  <action>
Create packages/ui/tokens.css by extracting all tokens from portal/src/app/globals.css verbatim. This file is the single source of truth — portal will IMPORT from here in Plan 03.

Structure:
1. :root block — all CSS custom properties (stone scale, brand colours, semantic aliases)
2. @theme inline block — Tailwind v4 color/font mappings (--color-* and --font-* only, no @import here)
3. Dark-mode overrides — same @media (prefers-color-scheme: dark) block
4. Utility classes — .grain, .img-zoom, .dust-line, .animate-fade-up, .animation-delay-*
5. @keyframes fade-up

IMPORTANT:
- Do NOT include `@import "tailwindcss"` — each app's globals.css does that
- Copy token values verbatim from portal/src/app/globals.css (hex values must match exactly)
- Include the SVG grain data-uri exactly as written in portal globals.css
- Add a CSS comment at the top: `/* Trackline UI — design tokens v1 */`
- The @theme inline block should list ALL colour tokens (stone-50 through stone-900, red-dust, red-dust-light, ochre, ochre-light, eucalypt, eucalypt-light, sky, background, foreground, muted, muted-foreground, accent, accent-light) and font aliases (--font-sans, --font-display)
  </action>
  <verify>
    <automated>grep -c "red-dust: #b5452a" "c:/Software code GITs/LandManagment Website/packages/ui/tokens.css"</automated>
  </verify>
  <acceptance_criteria>
    - grep "--red-dust: #b5452a" packages/ui/tokens.css returns a match
    - grep "--ochre: #c9913a" packages/ui/tokens.css returns a match
    - grep "--eucalypt: #4a7c59" packages/ui/tokens.css returns a match
    - grep "@theme inline" packages/ui/tokens.css returns a match
    - grep "grain" packages/ui/tokens.css returns a match (grain utility class present)
    - File does NOT contain "@import \"tailwindcss\""
  </acceptance_criteria>
  <done>tokens.css contains all Trackline brand tokens verbatim from portal globals.css with @theme inline block and utility classes — verified by grep</done>
</task>

</tasks>

<verification>
From workspace root (c:/Software code GITs/LandManagment Website):

```bash
# Package resolves
pnpm list --filter @trackline/ui

# Token values are correct (must match portal globals.css exactly)
grep "--red-dust: #b5452a" packages/ui/tokens.css
grep "--ochre: #c9913a" packages/ui/tokens.css
grep "--stone-50: #faf8f6" packages/ui/tokens.css

# No @import tailwindcss in tokens.css (apps handle that themselves)
grep "@import" packages/ui/tokens.css | grep -v "^$" && echo "FAIL — unexpected @import" || echo "OK"
```
</verification>

<success_criteria>
- @trackline/ui package is listed by pnpm and resolves via workspace:*
- tokens.css hex values are character-for-character identical to portal/src/app/globals.css
- cn() utility compiles and merges classes correctly
- No @import "tailwindcss" in tokens.css
</success_criteria>

<output>
After completion, create .planning/phases/04-shared-ui-system/04-01-SUMMARY.md
</output>
