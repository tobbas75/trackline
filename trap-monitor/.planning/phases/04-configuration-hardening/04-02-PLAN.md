---
phase: 04-configuration-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/src/main.cpp
  - firmware/src/config.h
  - backend/supabase/functions/ingest-sms/index.ts
autonomous: true
requirements: [CFG-01, CFG-04]

must_haves:
  truths:
    - "Firmware prints WARNING on serial if unit ID is still factory default TRAP_001"
    - "Firmware prints WARNING on serial if SMS number is still factory default +61400000000"
    - "Firmware prints WARNING on serial if command PIN is still factory default 0000"
    - "Firmware continues operating after warnings (fail-open for field recovery)"
    - "Invalid DEVICE_TIMEZONE value is detected, logged with examples, and gracefully defaults to Australia/Sydney"
    - "Valid DEVICE_TIMEZONE values are accepted and logged as confirmed"
    - "Missing DEVICE_TIMEZONE logs a warning and defaults to Australia/Sydney"
  artifacts:
    - path: "firmware/src/main.cpp"
      provides: "Boot-time config validation with serial warnings"
      contains: "CONFIG WARNING"
    - path: "firmware/src/config.h"
      provides: "Sentinel markers for detecting uncustomized factory defaults"
      contains: "FACTORY_UNIT_ID"
    - path: "backend/supabase/functions/ingest-sms/index.ts"
      provides: "IANA timezone validation for DEVICE_TIMEZONE"
      contains: "supportedValuesOf"
  key_links:
    - from: "firmware/src/main.cpp"
      to: "firmware/src/config.h"
      via: "FACTORY_UNIT_ID, FACTORY_SMS_NUMBER, FACTORY_CMD_PIN comparison in validateConfig()"
      pattern: "FACTORY_UNIT_ID|FACTORY_SMS_NUMBER|FACTORY_CMD_PIN"
    - from: "backend/supabase/functions/ingest-sms/index.ts"
      to: "Intl API"
      via: "Intl.supportedValuesOf('timeZone') for IANA validation"
      pattern: "supportedValuesOf"
---

<objective>
Verify and complete firmware boot-time configuration validation and IANA timezone validation in the SMS ingestion edge function.

Purpose: CFG-01 requires firmware to warn when deployed with factory defaults (unit ID "TRAP_001", SMS number "+61400000000", PIN "0000") so devices are not accidentally deployed unconfigured. CFG-04 requires DEVICE_TIMEZONE to be validated against the IANA timezone database so invalid values are caught instead of causing subtle timestamp parsing failures.

Output: Firmware `validateConfig()` function that checks 3 factory defaults with serial warnings + amber LED blink; edge function `getValidatedTimezone()` that validates against `Intl.supportedValuesOf("timeZone")` with graceful fallback.
</objective>

<execution_context>
@C:/GITS/trap-monitor/.claude/get-shit-done/workflows/execute-plan.md
@C:/GITS/trap-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@firmware/src/main.cpp
@firmware/src/config.h
@backend/supabase/functions/ingest-sms/index.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From firmware/src/config.h:
```cpp
#define DEFAULT_UNIT_ID       "TRAP_001"
#define DEFAULT_CMD_PIN       "0000"
#define DEFAULT_SMS_NUMBER    "+61400000000"

struct SystemConfig {
  String unitId          = DEFAULT_UNIT_ID;
  String smsNumber       = DEFAULT_SMS_NUMBER;
  String cmdPin          = DEFAULT_CMD_PIN;
  // ... other fields
  bool   armed           = true;
};
```

From firmware/src/main.cpp (setup flow):
```cpp
void setup() {
  Serial.begin(115200);
  // ... init LEDs, LittleFS
  storageLoadConfig(cfg);
  validateConfig(cfg);   // <-- CFG-01 validation point
  // ... init RTC, GPS, determine wake reason
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify firmware boot-time config validation (CFG-01)</name>
  <files>firmware/src/main.cpp, firmware/src/config.h</files>
  <action>
Verify the firmware config validation implementation satisfies CFG-01. Read both files and confirm all requirements are met.

**firmware/src/config.h — verify sentinel markers exist:**
1. Confirm `FACTORY_UNIT_ID` is defined, aliasing `DEFAULT_UNIT_ID` ("TRAP_001")
2. Confirm `FACTORY_SMS_NUMBER` is defined, aliasing `DEFAULT_SMS_NUMBER` ("+61400000000")
3. Confirm `FACTORY_CMD_PIN` is defined, aliasing `DEFAULT_CMD_PIN` ("0000")
4. Confirm these are defined after the `DEFAULT_*` defines and before or after the `SystemConfig` struct

**firmware/src/main.cpp — verify validateConfig() function:**
1. Confirm `validateConfig(const SystemConfig &cfg)` function exists
2. Confirm it checks `cfg.unitId == FACTORY_UNIT_ID` and prints a specific warning mentioning "TRAP_001"
3. Confirm it checks `cfg.smsNumber == FACTORY_SMS_NUMBER` and prints a specific warning mentioning "+61400000000"
4. Confirm it checks `cfg.cmdPin == FACTORY_CMD_PIN` and prints a specific warning mentioning "0000"
5. Confirm each warning uses the `[CONFIG WARNING]` prefix for grep-ability
6. Confirm a summary "NOT READY FOR DEPLOYMENT" message is printed when any defaults are detected
7. Confirm amber LED blink pattern (visual indication for when serial is not connected)
8. Confirm the function does NOT halt boot — warns only (fail-open for field recovery)

**firmware/src/main.cpp — verify call site in setup():**
1. Confirm `validateConfig(cfg)` is called immediately after `storageLoadConfig(cfg)` in `setup()`
2. Confirm it runs before RTC init, GPS init, and wake reason determination
3. Confirm it runs on every boot including deep sleep wake (since setup() runs on every wake)

**If any of the above are missing or incorrect, implement the fix.** Specifically:
- If `validateConfig()` doesn't exist, create it per the specification above
- If FACTORY_* sentinels don't exist in config.h, add them
- If the call in setup() is missing, add it after storageLoadConfig(cfg)
- Use Arduino `String` comparison with `==` operator (not `strcmp` — these are Arduino String objects)
- LED function uses `ledSet(LED_AMBER, BLINK_SOLID)` for on and `ledSet(LED_AMBER, OFF)` for off
  </action>
  <verify>
    <automated>cd C:/GITS/trap-monitor && grep -c "CONFIG WARNING" firmware/src/main.cpp && grep -c "FACTORY_" firmware/src/config.h && grep -c "validateConfig" firmware/src/main.cpp</automated>
  </verify>
  <done>
    - `validateConfig()` function exists in main.cpp, called in setup() after storageLoadConfig()
    - Checks cfg.unitId against FACTORY_UNIT_ID ("TRAP_001")
    - Checks cfg.smsNumber against FACTORY_SMS_NUMBER ("+61400000000")
    - Checks cfg.cmdPin against FACTORY_CMD_PIN ("0000")
    - Each uncustomized default produces a distinct warning with [CONFIG WARNING] prefix
    - Summary "NOT READY FOR DEPLOYMENT" message when any default is detected
    - Amber LED blink pattern provides visual indication
    - Firmware continues operating after warnings (does not halt boot)
    - FACTORY_UNIT_ID, FACTORY_SMS_NUMBER, FACTORY_CMD_PIN defined in config.h
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify IANA timezone validation in edge function (CFG-04)</name>
  <files>backend/supabase/functions/ingest-sms/index.ts</files>
  <action>
Verify the IANA timezone validation implementation satisfies CFG-04. Read the file and confirm all requirements are met.

**Verify getValidatedTimezone() function exists and is correct:**
1. Confirm function `getValidatedTimezone(): string` exists
2. Confirm it reads `Deno.env.get("DEVICE_TIMEZONE")`
3. Confirm **missing DEVICE_TIMEZONE** case:
   - Logs a `console.error` with `[CONFIG]` prefix
   - Error message mentions "DEVICE_TIMEZONE env var is not set"
   - Error message includes example values ("Australia/Brisbane", "Pacific/Auckland")
   - Returns "Australia/Sydney" as default (not crash)
4. Confirm **invalid DEVICE_TIMEZONE** case:
   - Uses `Intl.supportedValuesOf("timeZone")` to get valid IANA timezones
   - Checks if the provided value is in the valid list
   - Logs a `console.error` with `[CONFIG]` prefix and the invalid value
   - Error message includes example valid values
   - Returns "Australia/Sydney" as default (not crash)
5. Confirm **valid DEVICE_TIMEZONE** case:
   - Logs a `console.log` confirmation message with the validated value
   - Returns the validated timezone string
6. Confirm `const DEVICE_TIMEZONE = getValidatedTimezone()` is called at module scope
7. Confirm the old pattern `Deno.env.get("DEVICE_TIMEZONE") || "Australia/Sydney"` no longer exists (replaced by the validated function)

**Design verification:**
- The function must NOT crash the edge function on invalid timezone. Crashing would cause SMS message loss from field devices, which is worse than slightly wrong timestamps. Graceful fallback with error log is the correct behavior.
- The validation runs once at module scope (Deno.serve keeps the module alive between requests), so it only validates on cold start, not per-request — this is efficient.
- `Intl.supportedValuesOf("timeZone")` is available in Deno runtime — no external dependencies needed.

**Verify the old unvalidated line is gone:**
- The old line was: `const DEVICE_TIMEZONE = Deno.env.get("DEVICE_TIMEZONE") || "Australia/Sydney";`
- This should no longer exist — replaced by `const DEVICE_TIMEZONE = getValidatedTimezone();`

**If any of the above are missing or incorrect, implement the fix.** Specifically:
- If `getValidatedTimezone()` doesn't exist, create it per the specification above
- If the old unvalidated line still exists, replace it
- Ensure `Intl.supportedValuesOf("timeZone")` is used (not a hardcoded list)
- Ensure error messages include example timezone values for operator guidance
  </action>
  <verify>
    <automated>cd C:/GITS/trap-monitor && grep -c "supportedValuesOf" backend/supabase/functions/ingest-sms/index.ts && grep -c "getValidatedTimezone" backend/supabase/functions/ingest-sms/index.ts</automated>
  </verify>
  <done>
    - getValidatedTimezone() function exists in ingest-sms/index.ts
    - Missing DEVICE_TIMEZONE: logs error with examples, defaults to "Australia/Sydney"
    - Invalid DEVICE_TIMEZONE: logs error with examples, defaults to "Australia/Sydney"
    - Valid DEVICE_TIMEZONE: logs confirmation, returns the value
    - Uses Intl.supportedValuesOf("timeZone") — no external dependencies
    - Old unvalidated `Deno.env.get("DEVICE_TIMEZONE") || "Australia/Sydney"` line is gone
    - Edge function does NOT crash on invalid timezone — graceful fallback prevents message loss
    - Validation runs at module scope (once per cold start, not per request)
  </done>
</task>

</tasks>

<verification>
1. Firmware validation: `grep -n "CONFIG WARNING" firmware/src/main.cpp` — should show 4+ warning lines (3 specific + 1 summary)
2. Firmware call site: `grep -n "validateConfig" firmware/src/main.cpp` — should show function definition and call in setup()
3. Config sentinels: `grep -n "FACTORY_" firmware/src/config.h` — should show 3 sentinel defines
4. Edge function validation: `grep -n "supportedValuesOf" backend/supabase/functions/ingest-sms/index.ts` — should show IANA validation call
5. Edge function no old pattern: `grep -n 'DEVICE_TIMEZONE.*||' backend/supabase/functions/ingest-sms/index.ts` — should find nothing (old pattern removed)
6. Edge function function: `grep -n "getValidatedTimezone" backend/supabase/functions/ingest-sms/index.ts` — should show definition and call
</verification>

<success_criteria>
- Firmware prints specific [CONFIG WARNING] messages on serial when unitId, smsNumber, or cmdPin match factory defaults
- Firmware continues operating after warnings (does not halt — fail-open for field recovery)
- FACTORY_UNIT_ID, FACTORY_SMS_NUMBER, FACTORY_CMD_PIN sentinels defined in config.h
- validateConfig() called in setup() immediately after storageLoadConfig()
- Invalid DEVICE_TIMEZONE in edge function produces clear error log with example values
- Valid DEVICE_TIMEZONE is accepted and logged as confirmed
- Missing DEVICE_TIMEZONE logs warning and defaults to "Australia/Sydney"
- Edge function does not crash on invalid timezone (graceful degradation)
- No external dependencies added to either firmware or edge function
</success_criteria>

<output>
After completion, create `.planning/phases/04-configuration-hardening/04-02-SUMMARY.md`
</output>
