---
phase: 04-configuration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/env.ts
  - frontend/src/app/api/push/notify/route.ts
  - frontend/src/app/api/command/route.ts
  - frontend/src/lib/supabase/server.ts
  - frontend/src/lib/supabase/client.ts
  - frontend/src/middleware.ts
  - frontend/src/app/auth/signout/route.ts
  - frontend/src/app/auth/callback/route.ts
autonomous: true
requirements: [CFG-02, CFG-03]

must_haves:
  truths:
    - "Missing VAPID_EMAIL causes push endpoint to return 500 with clear error message instead of using fallback"
    - "Missing any required server env var at handler entry produces a clear error listing all missing vars"
    - "No env var is accessed with non-null assertion (!) without prior validation in any route file"
    - "VAPID_EMAIL requires mailto: prefix — no silent fallback to admin@example.com"
    - "DEFAULT_CMD_PIN has no fallback to 0000 — env var is required"
  artifacts:
    - path: "frontend/src/lib/env.ts"
      provides: "Centralized env validation with typed exports"
      exports: ["publicEnv", "getServerEnv", "validateVars"]
      min_lines: 30
    - path: "frontend/src/app/api/push/notify/route.ts"
      provides: "Push endpoint with required VAPID_EMAIL (no fallback)"
      contains: "senv.VAPID_EMAIL"
    - path: "frontend/src/app/api/command/route.ts"
      provides: "Command endpoint with required DEFAULT_CMD_PIN (no fallback)"
      contains: "senv.DEFAULT_CMD_PIN"
  key_links:
    - from: "frontend/src/lib/env.ts"
      to: "frontend/src/app/api/push/notify/route.ts"
      via: "import { getServerEnv, publicEnv } from '@/lib/env'"
      pattern: "env\\.VAPID_EMAIL"
    - from: "frontend/src/lib/env.ts"
      to: "frontend/src/app/api/command/route.ts"
      via: "import { getServerEnv, publicEnv } from '@/lib/env'"
      pattern: "senv\\.DEFAULT_CMD_PIN"
    - from: "frontend/src/lib/env.ts"
      to: "frontend/src/app/auth/signout/route.ts"
      via: "import { publicEnv } from '@/lib/env'"
      pattern: "publicEnv\\.NEXT_PUBLIC_SUPABASE"
---

<objective>
Ensure centralized environment variable validation is complete across the entire Next.js frontend — verify the env.ts module, confirm all API routes use validated env, and fix the remaining auth routes that still use raw `process.env.*!` assertions.

Purpose: CFG-02 requires VAPID_EMAIL to be required (no fallback). CFG-03 requires all env vars to be validated at startup with clear error messages. The core env.ts module and primary API routes are already wired, but auth routes (signout, callback) still use `process.env.*!` non-null assertions, which bypasses the centralized validation and could cause opaque runtime crashes if env vars are missing.

Output: All frontend route files use `publicEnv` or `getServerEnv()` from `@/lib/env` instead of raw `process.env` with `!` assertions. Zero `process.env.*!` patterns remain in route files.
</objective>

<execution_context>
@C:/GITS/trap-monitor/.claude/get-shit-done/workflows/execute-plan.md
@C:/GITS/trap-monitor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/lib/env.ts
@frontend/src/app/api/push/notify/route.ts
@frontend/src/app/api/command/route.ts
@frontend/src/lib/supabase/server.ts
@frontend/src/lib/supabase/client.ts
@frontend/src/middleware.ts
@frontend/src/app/auth/signout/route.ts
@frontend/src/app/auth/callback/route.ts
@frontend/.env.example

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/lib/env.ts:
```typescript
interface PublicEnv {
  NEXT_PUBLIC_SUPABASE_URL: string;
  NEXT_PUBLIC_SUPABASE_ANON_KEY: string;
  NEXT_PUBLIC_APP_URL: string;
  NEXT_PUBLIC_VAPID_PUBLIC_KEY: string;
}

interface ServerEnv {
  SUPABASE_SERVICE_ROLE_KEY: string;
  TELSTRA_API_TOKEN: string;
  DEFAULT_CMD_PIN: string;
  VAPID_PRIVATE_KEY: string;
  VAPID_EMAIL: string;
  SUPABASE_WEBHOOK_SECRET: string;
}

export const publicEnv: PublicEnv;
export function getServerEnv(): ServerEnv;
export function validateVars(spec: EnvSpec, source: Record<string, string | undefined>): { validated: Record<string, string>; errors: string[] };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify env.ts module and primary route wiring are correct</name>
  <files>frontend/src/lib/env.ts, frontend/src/app/api/push/notify/route.ts, frontend/src/app/api/command/route.ts, frontend/src/lib/supabase/server.ts, frontend/src/lib/supabase/client.ts, frontend/src/middleware.ts</files>
  <action>
Verify the existing implementation satisfies CFG-02 and CFG-03 requirements. Read each file and confirm:

**frontend/src/lib/env.ts (CFG-03 core):**
1. Confirm `publicEnv` is exported with all 4 NEXT_PUBLIC_ vars validated eagerly
2. Confirm `getServerEnv()` validates all 6 server vars lazily on first call
3. Confirm `VAPID_EMAIL` has rule `{ startsWith: "mailto:" }` (CFG-02)
4. Confirm `DEFAULT_CMD_PIN` is listed as required with no default
5. Confirm missing vars produce a single error listing ALL problems (not throwing on first)
6. Confirm `validateVars` is exported (needed for unit tests in Phase 7)

**frontend/src/app/api/push/notify/route.ts (CFG-02):**
1. Confirm `import { getServerEnv, publicEnv } from "@/lib/env"` is present
2. Confirm `senv.VAPID_EMAIL` is used directly in `webpush.setVapidDetails()` — no `?? "mailto:admin@example.com"` fallback
3. Confirm no `process.env.*!` assertions remain

**frontend/src/app/api/command/route.ts (CFG-02 related):**
1. Confirm `import { getServerEnv, publicEnv } from "@/lib/env"` is present
2. Confirm `senv.DEFAULT_CMD_PIN` is used — no `?? "0000"` fallback
3. Confirm `senv.TELSTRA_API_TOKEN` replaces raw process.env access
4. Confirm no `process.env.*!` assertions remain

**frontend/src/lib/supabase/server.ts, client.ts, middleware.ts (CFG-03):**
1. Confirm all three import `publicEnv` from `@/lib/env`
2. Confirm `publicEnv.NEXT_PUBLIC_SUPABASE_URL` and `publicEnv.NEXT_PUBLIC_SUPABASE_ANON_KEY` are used
3. Confirm no `process.env.*!` assertions remain

If any of the above checks fail, fix the issue. If all checks pass, proceed to Task 2.

**If env.ts needs any changes**, ensure:
- The `publicEnv` object uses literal `process.env.NEXT_PUBLIC_X` references (not dynamic access) because Next.js only inlines literal references at build time
- The `getServerEnv()` function caches after first successful validation
- Error messages follow the format: `Missing or invalid environment variables:\n  - VAR_NAME: missing\n  - VAR_NAME: invalid (must start with "...")`
  </action>
  <verify>
    <automated>cd C:/GITS/trap-monitor/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - env.ts exists with publicEnv (eager) and getServerEnv() (lazy) exports
    - VAPID_EMAIL has startsWith: "mailto:" validation rule, no fallback email
    - DEFAULT_CMD_PIN is required, no "0000" fallback
    - notify/route.ts uses senv.VAPID_EMAIL directly
    - command/route.ts uses senv.DEFAULT_CMD_PIN directly
    - server.ts, client.ts, middleware.ts all use publicEnv imports
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire auth routes to use validated env module</name>
  <files>frontend/src/app/auth/signout/route.ts, frontend/src/app/auth/callback/route.ts</files>
  <action>
Two auth route files still use raw `process.env.*!` non-null assertions, bypassing the centralized validation. Update them to use `publicEnv` from `@/lib/env`.

**frontend/src/app/auth/signout/route.ts:**
1. Add `import { publicEnv } from "@/lib/env";`
2. Replace `process.env.NEXT_PUBLIC_SUPABASE_URL!` (line 6) with `publicEnv.NEXT_PUBLIC_SUPABASE_URL`
3. Replace `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!` (line 7) with `publicEnv.NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. Remove the `!` non-null assertions — publicEnv values are already typed as `string` (never undefined)

**frontend/src/app/auth/callback/route.ts:**
1. Add `import { publicEnv } from "@/lib/env";`
2. Replace `process.env.NEXT_PUBLIC_SUPABASE_URL!` (line 11) with `publicEnv.NEXT_PUBLIC_SUPABASE_URL`
3. Replace `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!` (line 12) with `publicEnv.NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. Replace `process.env.NEXT_PUBLIC_APP_URL` (line 36) with `publicEnv.NEXT_PUBLIC_APP_URL`
5. Leave `process.env.NODE_ENV` (line 30) as-is — NODE_ENV is always set by Next.js, not a user-configured env var
6. Remove the `!` non-null assertions on the replaced lines

**Do NOT touch:**
- `frontend/src/lib/push.ts` — this is a client-side file where `process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY` is build-time inlined by Next.js. It works correctly without `!` (it just gets the inlined string). Changing it to use publicEnv import is safe but unnecessary — the build-time inlining handles it.
  </action>
  <verify>
    <automated>cd C:/GITS/trap-monitor && grep -rn "process\.env.*!" frontend/src/app/ frontend/src/lib/ frontend/src/middleware.ts --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v ".test." | grep -v "env.ts"</automated>
  </verify>
  <done>
    - signout/route.ts imports publicEnv and uses publicEnv.NEXT_PUBLIC_SUPABASE_URL (no !)
    - callback/route.ts imports publicEnv and uses publicEnv.NEXT_PUBLIC_SUPABASE_URL (no !)
    - callback/route.ts uses publicEnv.NEXT_PUBLIC_APP_URL for redirect (no raw process.env)
    - Zero `process.env.*!` non-null assertions remain in any route or lib file (excluding env.ts itself and test files)
    - TypeScript compiles without errors
    - process.env.NODE_ENV usage in callback/route.ts is preserved (always set by Next.js)
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — full TypeScript compilation passes
2. No `process.env.*!` assertions remain in route/lib files (excluding env.ts itself):
   `grep -rn "process\.env.*!" frontend/src/app/ frontend/src/lib/ frontend/src/middleware.ts --include="*.ts" | grep -v "env.ts" | grep -v ".test."`
3. No VAPID_EMAIL fallback:
   `grep -rn "admin@example" frontend/src/`
4. No DEFAULT_CMD_PIN "0000" fallback:
   `grep -rn '"0000"' frontend/src/app/api/command/route.ts`
5. All route files import from @/lib/env:
   `grep -rn "from.*@/lib/env" frontend/src/app/ frontend/src/lib/supabase/ frontend/src/middleware.ts --include="*.ts"`
</verification>

<success_criteria>
- `frontend/src/lib/env.ts` exports `publicEnv` (eager) and `getServerEnv()` (lazy) with all 10 env vars validated
- VAPID_EMAIL fallback "mailto:admin@example.com" does not exist anywhere in the codebase
- DEFAULT_CMD_PIN fallback "0000" does not exist in command/route.ts
- All route files (notify, command, signout, callback) and lib files (server.ts, client.ts, middleware.ts) use validated env module
- Zero `process.env.*!` non-null assertions in any production route or lib file
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-configuration-hardening/04-01-SUMMARY.md`
</output>
